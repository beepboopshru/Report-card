(function () {
  const content = window.LMS_CONTENT || {};
  function mergeCodes(lessonId, phase, codes) {
    if (!content[lessonId] || !content[lessonId][phase]) return;
    content[lessonId][phase].codes = {
      ...(content[lessonId][phase].codes || {}),
      ...codes
    };
  }

  mergeCodes("2-6-1", "explore", {"y2c6s1-explore-main-code": "#define LDR_PIN 2  // LDR sensor connected to D2\n#define LED_PIN 3  // LED connected to D3\nvoid setup() {\n pinMode(LDR_PIN, INPUT);\n pinMode(LED_PIN, OUTPUT);\n digitalWrite(LED_PIN, LOW);\n}\nvoid loop() {\n int ldrstate = digitalRead(LDR_PIN);\n if (ldrstate == HIGH) {\n  digitalWrite(LED_PIN, HIGH); // Turn LED ON\n } else {\n  digitalWrite(LED_PIN, LOW); // Turn LED OFF\n }\n}", "y2c6s1-explore-try-code": "#define LDR_PIN 2   // LDR sensor connected to D2\n#define LED1_PIN 3  // First LED connected to D3\n#define LED2_PIN 4  // Second LED connected to D4\nvoid setup() {\n  pinMode(LDR_PIN, INPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  digitalWrite(LED1_PIN, LOW);\n  digitalWrite(LED2_PIN, LOW);\n}\nvoid loop() {\n  int ldrstate = digitalRead(LDR_PIN);\n  if (ldrstate == HIGH) {\n    digitalWrite(LED1_PIN, HIGH); // LED1 ON\n    digitalWrite(LED2_PIN, HIGH); // LED2 ON\n  } else {\n    digitalWrite(LED1_PIN, LOW);  // LED1 OFF\n    digitalWrite(LED2_PIN, LOW);  // LED2 OFF\n  }\n}"});
  mergeCodes("2-6-1", "elaborate", {"y2c6s1-elaborate-project1-code": "// Session 1 Grade 6\n// Sheep / Cow - LDR Auto String LED\nconst int ldrPin = A0;\nconst int stringLED = 5;\nint darkValue = 500; // adjust after testing\nvoid setup() {\npinMode(stringLED, OUTPUT);\n}\nvoid loop() {\nint ldrValue = analogRead(ldrPin);\nif (ldrValue > darkValue) {\ndigitalWrite(stringLED, HIGH); // dark = LED ON\n} else {\ndigitalWrite(stringLED, LOW); // bright = LED OFF\n}\n}", "y2c6s1-elaborate-project2-code": "// Session 1 Grade 6\n// Sheep / Cow - LDR Auto String LED\nconst int ldrPin = A0;\nconst int stringLED = 5;\nint darkValue = 500; // adjust after testing\nvoid setup() {\npinMode(stringLED, OUTPUT);\n}\nvoid loop() {\nint ldrValue = analogRead(ldrPin);\nif (ldrValue > darkValue) {\ndigitalWrite(stringLED, HIGH); // dark = LED ON\n} else {\ndigitalWrite(stringLED, LOW); // bright = LED OFF\n}\n}"});
  mergeCodes("2-6-10", "explore", {"y2c6s10-explore-main-code": "#include <TM1637Display.h>\n#define CLK 8\n#define DIO 9\n#define IR_PIN 2   // IR sensor on D2\nTM1637Display display(CLK, DIO);\nint count = 0;\nint lastIRState = HIGH;\nvoid setup() {\n  pinMode(IR_PIN, INPUT);\n  display.setBrightness(7);\n  display.showNumberDec(0);\n}\nvoid loop() {\n  int irState = digitalRead(IR_PIN);\n  // Detect object (LOW = detected for most IR sensors)\n  if (irState == LOW && lastIRState == HIGH) {\n    count++;\n    if (count == 10) count = 0;\n    display.showNumberDec(count);\n    delay(200); // debounce\n  }\n  lastIRState = irState;\n}", "y2c6s10-explore-try-code": "#include <TM1637Display.h>\n#define CLK 8\n#define DIO 9\n#define IR_PIN 2     // IR sensor on D2\n#define LED_PIN 5    // LED on D5\nTM1637Display display(CLK, DIO);\nint count = 0;\nint lastIRState = HIGH;\nvoid setup() {\n  pinMode(IR_PIN, INPUT);\n  pinMode(LED_PIN, OUTPUT);\n  display.setBrightness(7);\n  display.showNumberDec(0);\n  digitalWrite(LED_PIN, LOW);\n}\nvoid loop() {\n  int irState = digitalRead(IR_PIN);\n  if (irState == LOW && lastIRState == HIGH) {\n    count++;\n    if (count == 10) count = 0;\n    display.showNumberDec(count);\n    digitalWrite(LED_PIN, HIGH); // LED ON\n    delay(200);                  // visible blink\n    digitalWrite(LED_PIN, LOW);  // LED OFF\n  }\n  lastIRState = irState;\n}"});
  mergeCodes("2-6-10", "elaborate", {"y2c6s10-elaborate-project1-code": "// Session 10 Grade 6\n// Smart Home With Counter and Automatic Night Light\n// LDR controls 1W LED\n// IR counts people entering and shows count on 7\nsegment\n#include <TM1637Display.h>\nconst int ldrSensor = A0;\nconst int irSensor = 2;\nconst int led = 6;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int IR_DETECTED = LOW; // change to HIGH if\nopposite\nint darkValue = 500; // adjust based on room light\nTM1637Display display(CLK, DIO);\nint personCount = 0;\nbool lastIR = HIGH;"});
  mergeCodes("2-6-2", "explore", {"y2c6s2-explore-main-code": "#define SOUND_PIN A0   // Analog output pin from sound sensor\nvoid setup() {\n Serial.begin(9600); // Start the Serial Monitor\n}\nvoid loop() {\n int soundValue = analogRead(SOUND_PIN); // Read the sound sensor value (0-1023)\n Serial.print(\"Sound value: \");\n Serial.println(soundValue); // Print the sound value\n delay(100); // Small delay to smooth the readings\n}", "y2c6s2-explore-try-code": "#define SOUND_PIN A0    // Sound sensor on A0\n#define BUZZER_PIN 5    // Buzzer on D5\nvoid setup() {\n  Serial.begin(9600);\n  pinMode(BUZZER_PIN, OUTPUT);\n}\nvoid loop() {\n  int soundValue = analogRead(SOUND_PIN); // Read sound (0–1023)\n  Serial.print(\"Sound value: \");\n  Serial.println(soundValue);\n  // If sound is loud, turn buzzer ON\n  if (soundValue > 500) {\n    digitalWrite(BUZZER_PIN, HIGH); // Buzzer ON\n  } else {\n    digitalWrite(BUZZER_PIN, LOW);  // Buzzer OFF\n  }\n  delay(100);\n}"});
  mergeCodes("2-6-2", "elaborate", {"y2c6s2-elaborate-project1-code": "// Session 2 Grade 6\n// Birthday Party / Cake\n// Sound Sensor + LED Only\n// Blow / sound detected = LED OFF, then reset ON\nconst int soundSensor = A0;\nconst int led = 6;\nint soundLimit = 600; // adjust after testing\nvoid setup() {\npinMode(led, OUTPUT);\ndigitalWrite(led, HIGH); // candle LED ON initially\n}\nvoid loop() {\nint soundValue = analogRead(soundSensor);\nif (soundValue > soundLimit) {\ndigitalWrite(led, LOW); // candle OFF\ndelay(3000); // stay OFF for 3 seconds\ndigitalWrite(led, HIGH); // reset candle ON\n}\n}"});
  mergeCodes("2-6-3", "explore", {"y2c6s3-explore-main-code": "#define TILT_PIN 2 // Tilt sensor connected to D2\n#define BUZZER_PIN 3 // Buzzer connected to D3\nvoid setup() {\n pinMode(TILT_PIN, INPUT);\n pinMode(BUZZER_PIN, OUTPUT);\n digitalWrite(BUZZER_PIN, LOW);\n}\nvoid loop() {\n int tiltState = digitalRead(TILT_PIN);\n if (tiltState == HIGH) {\n  digitalWrite(BUZZER_PIN, HIGH); // Turn buzzer ON\n } else {\n  digitalWrite(BUZZER_PIN, LOW); // Turn buzzer OFF\n }\n}", "y2c6s3-explore-try-code": "#define TILT_PIN 2     // Tilt sensor on D2\n#define BUZZER_PIN 3   // Buzzer on D3\n#define LED_PIN 5      // LED on D5\nvoid setup() {\n  pinMode(TILT_PIN, INPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  pinMode(LED_PIN, OUTPUT);\n  digitalWrite(BUZZER_PIN, LOW);\n  digitalWrite(LED_PIN, LOW);\n}\nvoid loop() {\n  int tiltState = digitalRead(TILT_PIN);\n  if (tiltState == HIGH) {\n    digitalWrite(BUZZER_PIN, HIGH); // Buzzer ON\n    digitalWrite(LED_PIN, HIGH);    // LED ON\n  } else {\n    digitalWrite(BUZZER_PIN, LOW);  // Buzzer OFF\n    digitalWrite(LED_PIN, LOW);     // LED OFF\n  }\n}"});
  mergeCodes("2-6-3", "elaborate", {"y2c6s3-elaborate-project1-code": "// Session 3 Grade 6\n// Exhaust Fan\n// Tilt sensor tilted = fan motor ON\nconst int tiltSensor = 2;\nconst int motor = 5;\nconst int TILT_ACTIVE = HIGH; // change to LOW if opposite\nvoid setup() {\npinMode(tiltSensor, INPUT);\npinMode(motor, OUTPUT);\n}\nvoid loop() {\nif (digitalRead(tiltSensor) == TILT_ACTIVE) {\ndigitalWrite(motor, HIGH);\n} else {\ndigitalWrite(motor, LOW);\n}\n}", "y2c6s3-elaborate-project2-code": "//Session 3 Grade 6\n// Bike Stand\n// Stand open = LED OFF\n// Stand released = LED ON\nconst int tiltSensor = 2;\nconst int led = 6;\nconst int STAND_OPEN = HIGH; // change to LOW if opposite\nvoid setup() {\npinMode(tiltSensor, INPUT);\npinMode(led, OUTPUT);\n}\nvoid loop() {\nif (digitalRead(tiltSensor) == STAND_OPEN) {\ndigitalWrite(led, LOW);\n} else {\ndigitalWrite(led, HIGH);\n}\n}"});
  mergeCodes("2-6-4", "explore", {"y2c6s4-explore-main-code": "#define HALL_PIN 2  // Hall sensor output pin\n#define LED_PIN 3  // LED pin\nvoid setup() {\n pinMode(HALL_PIN, INPUT);\n pinMode(LED_PIN, OUTPUT);\n}\nvoid loop() {\n int hallState = digitalRead(HALL_PIN);\n if (hallState == LOW) {    // Magnet detected (output LOW)\n  digitalWrite(LED_PIN, HIGH); // Turn ON LED\n } else {\n  digitalWrite(LED_PIN, LOW); // Turn OFF LED\n }\n}", "y2c6s4-explore-try-code": "#define HALL_PIN 2     // Hall sensor output pin\n#define LED1_PIN 3     // First LED on D3\n#define LED2_PIN 4     // Second LED on D4\nvoid setup() {\n  pinMode(HALL_PIN, INPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n}\nvoid loop() {\n  int hallState = digitalRead(HALL_PIN);\n  if (hallState == LOW) {          // Magnet detected\n    digitalWrite(LED1_PIN, HIGH); // LED1 ON\n    digitalWrite(LED2_PIN, HIGH); // LED2 ON\n  } else {\n    digitalWrite(LED1_PIN, LOW);  // LED1 OFF\n    digitalWrite(LED2_PIN, LOW);  // LED2 OFF\n  }\n}"});
  mergeCodes("2-6-4", "elaborate", {"y2c6s4-elaborate-project1-code": "const int magneticSensor = A0;\nconst int motor = 5;\nconst int MAGNET_DETECTED = LOW; // change\nto HIGH if opposite\nvoid setup() {\npinMode(magneticSensor, INPUT);\npinMode(motor, OUTPUT);\n}\nvoid loop() {\nif (digitalRead(magneticSensor) ==\nMAGNET_DETECTED) {\ndigitalWrite(motor, HIGH);\n} else {\ndigitalWrite(motor, LOW);\n}\n}", "y2c6s4-elaborate-project2-code": "const int magneticSensor = A0;\nconst int motor = 5;\nconst int MAGNET_DETECTED = LOW; // change\nto HIGH if opposite\nvoid setup() {\npinMode(magneticSensor, INPUT);\npinMode(motor, OUTPUT);\n}\nvoid loop() {\nif (digitalRead(magneticSensor) ==\nMAGNET_DETECTED) {\ndigitalWrite(motor, HIGH);\n} else {\ndigitalWrite(motor, LOW);\n}\n}"});
  mergeCodes("2-6-5", "explore", {"y2c6s5-explore-main-code": "#define IR_PIN 2  // IR sensor connected to D2\n#define LED_PIN 6  // LED connected to D3\nvoid setup() {\n pinMode(IR_PIN, INPUT);\n pinMode(LED_PIN, OUTPUT);\n digitalWrite(LED_PIN, LOW);\n}\nvoid loop() {\n int IRState = digitalRead(IR_PIN);\n if (IRState == LOW) {\n  digitalWrite(LED_PIN, HIGH); // Turn LED ON\n } else {\n  digitalWrite(LED_PIN, LOW); // Turn LED OFF\n }\n}", "y2c6s5-explore-try-code": "#define IR_PIN   2    // IR sensor connected to D2\n#define LED1_PIN 3    // LED1 on D3\n#define LED2_PIN 4    // LED2 on D4\nvoid setup() {\n  pinMode(IR_PIN, INPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  digitalWrite(LED1_PIN, LOW);\n  digitalWrite(LED2_PIN, LOW);\n}\nvoid loop() {\n  int IRState = digitalRead(IR_PIN);\n  if (IRState == LOW) {\n    digitalWrite(LED1_PIN, HIGH); // LED1 ON\n    digitalWrite(LED2_PIN, HIGH); // LED2 ON\n  } else {\n    digitalWrite(LED1_PIN, LOW);  // LED1 OFF\n    digitalWrite(LED2_PIN, LOW);  // LED2 OFF\n  }\n}"});
  mergeCodes("2-6-5", "elaborate", {"y2c6s5-elaborate-project1-code": "// Session 5 Grade 6 Project 1\n// Wall Parking Alert\n// Object near wall = buzzer ON\nconst int irSensor = 2;\nconst int buzzer = 5;\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nvoid setup() {\npinMode(irSensor, INPUT);\npinMode(buzzer, OUTPUT);\ndigitalWrite(buzzer, LOW);\n}\nvoid loop() {\nif (digitalRead(irSensor) == IR_DETECTED) {\ndigitalWrite(buzzer, HIGH);\n} else {\ndigitalWrite(buzzer, LOW);\n}\n}", "y2c6s5-elaborate-project2-code": "// Session 5 Grade 6 Project 2\n// Smart Trashcan Lid\n// Person near dustbin = lid opens for 5 seconds\n#include <Servo.h>\nconst int irSensor = 2;\nconst int servoPin = 3;\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nServo lidServo;\nvoid setup() {\npinMode(irSensor, INPUT);\nlidServo.attach(servoPin);\nlidServo.write(0); // lid closed\n}\nvoid loop() {\nif (digitalRead(irSensor) == IR_DETECTED) {\nlidServo.write(90); // lid open\ndelay(5000);\nlidServo.write(0); // lid close\ndelay(500);\n}\n}"});
  mergeCodes("2-6-6", "explore", {"y2c6s6-explore-main-code": "#include <Servo.h>\n#define SERVO_PIN 2  // Servo signal connected to pin 2\nServo myServo;\nvoid setup() {\n myServo.attach(SERVO_PIN);\n}\nvoid loop() {\n for (int angle = 0; angle <= 180; angle++) {\n  myServo.write(angle);\n  delay(15);\n }\n for (int angle = 180; angle >= 0; angle--) {\n  myServo.write(angle);\n  delay(15);\n }\n}", "y2c6s6-explore-try-code": "#include <Servo.h>\n#define SERVO_PIN 2   // Servo on D2\nServo myServo;\nvoid setup() {\n  myServo.attach(SERVO_PIN);\n}\nvoid loop() {\n  // Move from 0° to 90°\n  for (int angle = 0; angle <= 90; angle++) {\n    myServo.write(angle);\n    delay(15);\n  }\n  // Move back from 90° to 0°\n  for (int angle = 90; angle >= 0; angle--) {\n    myServo.write(angle);\n    delay(15);\n  }\n}"});
  mergeCodes("2-6-6", "elaborate", {"y2c6s6-elaborate-project1-code": "// Session 6 Grade 6 Project 1\n// Dog Tail Movement\n// IR detected = dog tail moves\n#include <Servo.h>\nconst int irSensor = 2;\nconst int servoPin = 3;\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nServo tailServo;\nvoid setup() {\npinMode(irSensor, INPUT);\ntailServo.attach(servoPin);\ntailServo.write(90);\n}\nvoid loop() {\nif (digitalRead(irSensor) == IR_DETECTED) {\nfor (int i = 0; i < 3; i++) {\ntailServo.write(40);\ndelay(300);\ntailServo.write(140);\ndelay(300);\n}\ntailServo.write(90);\n}\n}", "y2c6s6-elaborate-project2-code": "// Session 6 Grade 6 Project 2\n// Crocodile Face Movement\n// IR detected = crocodile face moves\n#include <Servo.h>\nconst int irSensor = 2;\nconst int servoPin = 3;\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nServo faceServo;\nvoid setup() {\npinMode(irSensor, INPUT);\nfaceServo.attach(servoPin);\nfaceServo.write(0);\n}\nvoid loop() {\nif (digitalRead(irSensor) == IR_DETECTED) {\nfaceServo.write(90);\ndelay(1000);\nfaceServo.write(0);\ndelay(500);\n}\n}"});
  mergeCodes("2-6-7", "explore", {"y2c6s7-explore-main-code": "#include <TM1637Display.h> // Include library for 4-digit 7-segment display\n#define CLK 8 // Clock pin connected to D8\n#define DIO 9 // Data pin connected to D9\nTM1637Display display(CLK, DIO); // Create display object\nvoid setup() {\n display.setBrightness(7); // Set display brightness (range 0 to 7)\n}\nvoid loop() {\n static int count = 0; // Start from 0 and store the count\n display.showNumberDec(count); // Show the number on display\n delay(500);          // Wait for 0.5 seconds\n count++; // Increase the number\n if (count == 10) { // Reset to 0 after 9\n  count = 0;\n }\n}", "y2c6s7-explore-try-code": "#include <TM1637Display.h>\n#define CLK 8\n#define DIO 9\nTM1637Display display(CLK, DIO);\nvoid setup() {\n  display.setBrightness(7);\n}\nvoid loop() {\n  static int count = 0;\n  display.showNumberDec(count);\n  delay(500);\n  count = (count + 1) % 10;  // Automatically loops 0 → 9 → 0\n}"});
  mergeCodes("2-6-7", "elaborate", {"y2c6s7-elaborate-project1-code": "// Session 7 Grade 6 Project 1\n// Smart Car Counter\n// Each car passing IR sensor = count increases\n#include <TM1637Display.h>\nconst int irSensor = 2;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int IR_DETECTED = LOW;   // change to HIGH if opposite\nTM1637Display display(CLK, DIO);\nint carCount = 0;\nbool lastIR = HIGH;\nvoid setup() {\npinMode(irSensor, INPUT);\ndisplay.setBrightness(7);\ndisplay.showNumberDec(0, true);\n}\nvoid loop() {\nbool currentIR = digitalRead(irSensor);\nif (currentIR == IR_DETECTED && lastIR != IR_DETECTED) {\ncarCount++;\nif (carCount > 9999) carCount = 0;\ndisplay.showNumberDec(carCount, true);\ndelay(300);\n}\nlastIR = currentIR;\n}", "y2c6s7-elaborate-project2-code": "// Session 7 Grade 6 Project 2\n// Speedometer\n// Car crosses IR1 then IR2 = speed value shown on 7 segment\n#include <TM1637Display.h>\nconst int irSensor1 = 2;\nconst int irSensor2 = 3;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nconst float distanceCM = 20.0; // distance between two IR sensors\nTM1637Display display(CLK, DIO);\nunsigned long startTime = 0;\nbool timingStarted = false;\nbool lastIR1 = HIGH;\nbool lastIR2 = HIGH;\nvoid setup() {\npinMode(irSensor1, INPUT);\npinMode(irSensor2, INPUT);\ndisplay.setBrightness(7);\ndisplay.showNumberDec(0, true);\n}", "y2c6s7-elaborate-project3-code": "#include <TM1637Display.h>\n#define CLK 8\n#define DIO 9\nTM1637Display display(CLK, DIO);\nvoid setup() {\n  display.setBrightness(7);\n}\nvoid loop() {\n  static int count = 0;\n  display.showNumberDec(count);\n  delay(500);\n  count = (count + 1) % 10;  // Automatically loops 0 → 9 → 0\n}"});
  mergeCodes("2-6-8", "explore", {"y2c6s8-explore-main-code": "#define TOUCH_PIN 2     // Touch sensor connected to D2\n#define BUTTON_PIN 5    // Push button connected to D5\n#define LED_PIN 6       // 1W LED connected to D6\nbool ledState = false;  // Stores LED state\nvoid setup() {\n  pinMode(TOUCH_PIN, INPUT);\n  pinMode(BUTTON_PIN, INPUT_PULLUP);  // Button connected to GND when pressed\n  pinMode(LED_PIN, OUTPUT);\n  digitalWrite(LED_PIN, LOW); // LED initially OFF\n}\nvoid loop() {\n  int touchState = digitalRead(TOUCH_PIN);\n  int buttonState = digitalRead(BUTTON_PIN);\n  // If touch sensor is touched, turn LED ON\n  if (touchState == HIGH) {\n    ledState = true;\n  }\n  // If push button is pressed, turn LED OFF\n  if (buttonState == LOW) {   // because INPUT_PULLUP is used\n    ledState = false;\n  }\n  // Update LED output\n  digitalWrite(LED_PIN, ledState ? HIGH : LOW);\n}", "y2c6s8-explore-try-code": "#define TOUCH_PIN 2     // Touch sensor on D2\n#define LED1_PIN 3      // First LED on D3\n#define LED2_PIN 4      // Second LED on D4\nvoid setup() {\n  pinMode(TOUCH_PIN, INPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  digitalWrite(LED1_PIN, LOW);\n  digitalWrite(LED2_PIN, LOW);\n}\nvoid loop() {\n  int touchState = digitalRead(TOUCH_PIN);\n  if (touchState == HIGH) {\n    digitalWrite(LED1_PIN, HIGH); // LED1 ON\n    digitalWrite(LED2_PIN, HIGH); // LED2 ON\n  } else {\n    digitalWrite(LED1_PIN, LOW);  // LED1 OFF\n    digitalWrite(LED2_PIN, LOW);  // LED2 OFF\n  }\n}"});
  mergeCodes("2-6-8", "elaborate", {"y2c6s8-elaborate-project1-code": "// Session 8 Grade 6\n// Car Back Safety System\n// IR detects object = buzzer ON\n// Push button turns alarm OFF until object is removed\nconst int irSensor = 2;\nconst int pushButton = 5;\nconst int buzzer = 7;\nconst int IR_DETECTED = LOW;   // change to HIGH if opposite\nbool alarmMuted = false;\nvoid setup() {\npinMode(irSensor, INPUT);\npinMode(pushButton, INPUT_PULLUP);\npinMode(buzzer, OUTPUT);\n}\nvoid loop() {\nbool objectDetected = digitalRead(irSensor) == IR_DETECTED;\nif (digitalRead(pushButton) == LOW) {\nalarmMuted = true;\ndigitalWrite(buzzer, LOW);\ndelay(300);\n}\nif (!objectDetected) {\nalarmMuted = false;\n}\nif (objectDetected && !alarmMuted) {\ndigitalWrite(buzzer, HIGH);\n} else {\ndigitalWrite(buzzer, LOW);\n}\n}"});
  mergeCodes("2-6-9", "explore", {"y2c6s9-explore-main-code": "#define LASER_PIN 2   // Laser diode on D2\n#define LDR_PIN 3     // LDR sensor on D3\n#define LED_PIN 5     // LED on D5\nvoid setup() {\n  pinMode(LASER_PIN, OUTPUT);\n  pinMode(LDR_PIN, INPUT);\n  pinMode(LED_PIN, OUTPUT);\n  digitalWrite(LASER_PIN, HIGH); // Laser ON\n  digitalWrite(LED_PIN, LOW);    // LED OFF initially\n}\nvoid loop() {\n  int ldrState = digitalRead(LDR_PIN);\n  if (ldrState == HIGH) {\n    // Laser hitting LDR\n    digitalWrite(LED_PIN, HIGH);  // LED ON\n  } else {\n    // Laser NOT hitting LDR\n    digitalWrite(LED_PIN, LOW);   // LED OFF\n  }\n}", "y2c6s9-explore-try-code": "#define LASER_PIN 2   // Laser diode on D2\n#define LDR_PIN 3     // LDR sensor on D3\n#define BUZZER_PIN 5  // Buzzer on D5\nvoid setup() {\n  pinMode(LASER_PIN, OUTPUT);\n  pinMode(LDR_PIN, INPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  digitalWrite(LASER_PIN, HIGH); // Laser ON\n  digitalWrite(BUZZER_PIN, LOW); // Buzzer OFF initially\n}\nvoid loop() {\n  int ldrState = digitalRead(LDR_PIN);\n  if (ldrState == HIGH) {\n    // Laser hitting LDR\n    digitalWrite(BUZZER_PIN, HIGH); // Buzzer ON\n  } else {\n    // Laser NOT hitting LDR\n    digitalWrite(BUZZER_PIN, LOW);  // Buzzer OFF\n  }\n}"});
  mergeCodes("2-6-9", "elaborate", {"y2c6s9-elaborate-project1-code": "// Session 9 Grade 6\n// Smart Gate\n// Vehicle detected = count increases, gate opens, green LED ON\n// No vehicle = gate closed, red LED ON\n#include <Servo.h>\n#include <TM1637Display.h>\nconst int irSensor = 2;\nconst int servoPin = 4;\nconst int redLED = 10;\nconst int greenLED = 11;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nServo gateServo;\nTM1637Display display(CLK, DIO);\nint vehicleCount = 0;\nbool lastIR = HIGH;\nvoid setup() {\npinMode(irSensor, INPUT);\npinMode(redLED, OUTPUT);\npinMode(greenLED, OUTPUT);\ngateServo.attach(servoPin);\ngateServo.write(0);\ndisplay.setBrightness(7);\ndisplay.showNumberDec(0, true);\ndigitalWrite(redLED, HIGH);\ndigitalWrite(greenLED, LOW);\n}\nvoid loop() {\nbool currentIR = digitalRead(irSensor);\nif (currentIR == IR_DETECTED && lastIR != IR_DETECTED) {\nvehicleCount++;\nif (vehicleCount > 9999) vehicleCount = 0;\ndisplay.showNumberDec(vehicleCount, true);\ndigitalWrite(redLED, LOW);\ndigitalWrite(greenLED, HIGH);\ngateServo.write(90);\ndelay(3000);\ngateServo.write(0);\ndigitalWrite(greenLED, LOW);\ndigitalWrite(redLED, HIGH);\ndelay(300);\n}\nlastIR = currentIR;\n}"});
  mergeCodes("2-7-1", "explore", {"y2c7s1-explore-main-code": "#define LDR_PIN 2 // LDR sensor connected to D2\n#define LED_PIN1 6 // LED connected to D6\n#define LED_PIN2 7 // LED connected to D7 (works opposite to D6)\nvoid setup() {\n pinMode(LDR_PIN, INPUT);\n pinMode(LED_PIN1, OUTPUT);\n pinMode(LED_PIN2, OUTPUT);\n digitalWrite(LED_PIN1, LOW);\n digitalWrite(LED_PIN2, LOW);\n}\nvoid loop() {\n int ldrstate = digitalRead(LDR_PIN);\n if (ldrstate == HIGH) {\n  digitalWrite(LED_PIN1, HIGH); // Turn LED at D6 ON\n  digitalWrite(LED_PIN2, LOW); // Turn LED at D7 OFF (opposite behavior)\n } else {\n  digitalWrite(LED_PIN1, LOW); // Turn LED at D6 OFF\n  digitalWrite(LED_PIN2, HIGH); // Turn LED at D7 ON (opposite behavior)\n }\n}", "y2c7s1-explore-try-code": "#define LDR_PIN 2     // LDR sensor connected to D2\n#define LED_PIN 6     // Only one LED on D6\n#define BUZZER_PIN 5  // Buzzer on D5\nvoid setup() {\n  pinMode(LDR_PIN, INPUT);\n  pinMode(LED_PIN, OUTPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  digitalWrite(LED_PIN, LOW);\n  digitalWrite(BUZZER_PIN, LOW);\n}\nvoid loop() {\n  int ldrstate = digitalRead(LDR_PIN);\n  if (ldrstate == HIGH) {\n    digitalWrite(LED_PIN, HIGH);    // LED ON\n    digitalWrite(BUZZER_PIN, HIGH); // Buzzer ON\n  } else {\n    digitalWrite(LED_PIN, LOW);     // LED OFF\n    digitalWrite(BUZZER_PIN, LOW);  // Buzzer OFF\n  }\n}"});
  mergeCodes("2-7-1", "elaborate", {"y2c7s1-elaborate-project1-code": "// Session 1 Grade 7\n// Iron Man / Lamp - LDR Auto 1W LED\nconst int ldrPin = A0;\nconst int led = 6;\nint darkValue = 500; // adjust after testing\nvoid setup() {\npinMode(led, OUTPUT);\n}\nvoid loop() {\nint ldrValue = analogRead(ldrPin);\nif (ldrValue > darkValue) {\ndigitalWrite(led, HIGH);\n} else {\ndigitalWrite(led, LOW);\n}\n}", "y2c7s1-elaborate-project2-code": "#define LDR_PIN 2     // LDR sensor connected to D2\n#define LED_PIN 6     // Only one LED on D6\n#define BUZZER_PIN 5  // Buzzer on D5\nvoid setup() {\n  pinMode(LDR_PIN, INPUT);\n  pinMode(LED_PIN, OUTPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  digitalWrite(LED_PIN, LOW);\n  digitalWrite(BUZZER_PIN, LOW);\n}\nvoid loop() {\n  int ldrstate = digitalRead(LDR_PIN);\n  if (ldrstate == HIGH) {\n    digitalWrite(LED_PIN, HIGH);    // LED ON\n    digitalWrite(BUZZER_PIN, HIGH); // Buzzer ON\n  } else {\n    digitalWrite(LED_PIN, LOW);     // LED OFF\n    digitalWrite(BUZZER_PIN, LOW);  // Buzzer OFF\n  }\n}"});
  mergeCodes("2-7-10", "explore", {"y2c7s10-explore-main-code": "#define LDR_PIN 2 // LDR sensor connected to D2\n#define LED_PIN1 6 // LED connected to D6\n#define LED_PIN2 7 // LED connected to D7 (works opposite to D6)\nvoid setup() {\n pinMode(LDR_PIN, INPUT);\n pinMode(LED_PIN1, OUTPUT);\n pinMode(LED_PIN2, OUTPUT);\n digitalWrite(LED_PIN1, LOW);\n digitalWrite(LED_PIN2, LOW);\n}\nvoid loop() {\n int ldrstate = digitalRead(LDR_PIN);\n if (ldrstate == HIGH) {\n  digitalWrite(LED_PIN1, HIGH); // Turn LED at D6 ON\n  digitalWrite(LED_PIN2, LOW); // Turn LED at D7 OFF (opposite behavior)\n } else {\n  digitalWrite(LED_PIN1, LOW); // Turn LED at D6 OFF\n  digitalWrite(LED_PIN2, HIGH); // Turn LED at D7 ON (opposite behavior)\n }\n}", "y2c7s10-explore-try-code": "#define LDR_PIN 2     // LDR sensor on D2\n#define BUZZ1_PIN 6   // Buzzer 1 on D6\n#define BUZZ2_PIN 7   // Buzzer 2 on D7 (opposite)\nvoid setup() {\n  pinMode(LDR_PIN, INPUT);\n  pinMode(BUZZ1_PIN, OUTPUT);\n  pinMode(BUZZ2_PIN, OUTPUT);\n  digitalWrite(BUZZ1_PIN, LOW);\n  digitalWrite(BUZZ2_PIN, LOW);\n}\nvoid loop() {\n  int ldrstate = digitalRead(LDR_PIN);\n  if (ldrstate == HIGH) {\n    digitalWrite(BUZZ1_PIN, HIGH); // Buzzer1 ON\n    digitalWrite(BUZZ2_PIN, LOW);  // Buzzer2 OFF\n  } else {\n    digitalWrite(BUZZ1_PIN, LOW);  // Buzzer1 OFF\n    digitalWrite(BUZZ2_PIN, HIGH); // Buzzer2 ON\n  }\n}"});
  mergeCodes("2-7-10", "elaborate", {"y2c7s10-elaborate-project1-code": "// Session 10 Grade 7\n// Hospital Patient Call System\n// Touch sensor = doctor calls next patient, red status ON\n// Patient taps magnet card to Hall sensor = status changes to green\n// D12 is kept always HIGH for RGB project\n// Hall sensor is used as DIGITAL sensor with digitalRead(A0)\n#include <TM1637Display.h>\nconst int touchSensor = 2;\nconst int hallSensor = A0;\nconst int redPin = 10;\nconst int greenPin = 11;\nconst int bluePin = 12;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int TOUCH_ACTIVE = HIGH; // change to LOW if opposite\nconst int MAGNET_DETECTED = LOW; // change to HIGH if opposite\nTM1637Display display(CLK, DIO);\nint patientNumber = 0;\nbool patientInside = false;\nbool lastTouch = LOW;\nbool lastMagnet = HIGH;\nvoid setup() {\npinMode(touchSensor, INPUT);\npinMode(hallSensor, INPUT);\npinMode(redPin, OUTPUT);\npinMode(greenPin, OUTPUT);\npinMode(bluePin, OUTPUT);\ndigitalWrite(bluePin, HIGH); // D12 always HIGH\ndisplay.setBrightness(7);\ndisplay.showNumberDec(0, true);\nshowGreenStatus();\n}\nvoid loop() {\ndigitalWrite(bluePin, HIGH); // keep D12 always HIGH\nbool touchNow = digitalRead(touchSensor);\nbool magnetNow = digitalRead(hallSensor);\nif (touchNow == TOUCH_ACTIVE && lastTouch != TOUCH_ACTIVE) {\npatientNumber++;\nif (patientNumber > 9999) patientNumber = 1;\npatientInside = true;\ndisplay.showNumberDec(patientNumber, true);\nshowRedStatus();\ndelay(300);\n}\nif (magnetNow == MAGNET_DETECTED && lastMagnet != MAGNET_DETECTED) {\npatientInside = false;\nshowGreenStatus();\ndelay(300);\n}\nlastTouch = touchNow;\nlastMagnet = magnetNow;\n}\nvoid showRedStatus() {\ndigitalWrite(redPin, HIGH);\ndigitalWrite(greenPin, LOW);\ndigitalWrite(bluePin, HIGH);\n}\nvoid showGreenStatus() {\ndigitalWrite(redPin, LOW);\ndigitalWrite(greenPin, HIGH);\ndigitalWrite(bluePin, HIGH);\n}"});
  mergeCodes("2-7-2", "explore", {"y2c7s2-explore-main-code": "#define SOUND_PIN A3 // Analog output pin from sound sensor\n#define LED_PIN 5   // LED connected to D5\nvoid setup() {\n Serial.begin(9600); // Start the Serial Monitor\n pinMode(LED_PIN, OUTPUT);\n}\nvoid loop() {\n int soundValue = analogRead(SOUND_PIN); // Read the sound sensor value (0-1023)\n Serial.print(\"Sound value: \");\n Serial.println(soundValue);      // Print the sound value\n int brightness = map(soundValue, 0, 1023, 0, 255); // Map to PWM range\n analogWrite(LED_PIN, brightness);         // Adjust LED brightness\n delay(100); // Small delay to smooth the readings\n}", "y2c7s2-explore-try-code": "#define SOUND_PIN A3 // Analog output pin from sound sensor\n#define LED_PIN1 5   // LED1 connected to D5\n#define LED_PIN2 6   // LED2 connected to D6\nvoid setup() {\n Serial.begin(9600); // Start the Serial Monitor\n pinMode(LED_PIN1, OUTPUT);\n pinMode(LED_PIN2, OUTPUT);\n}\nvoid loop() {\n int soundValue = analogRead(SOUND_PIN); // Read the sound sensor value (0-1023)\n Serial.print(\"Sound value: \");\n Serial.println(soundValue);      // Print the sound value\n int brightness = map(soundValue, 0, 1023, 0, 255); // Map to PWM range\n analogWrite(LED_PIN1, brightness); // Control LED1 brightness\n analogWrite(LED_PIN2, brightness); // Control LED2 brightness\n delay(100); // Small delay to smooth the readings\n}"});
  mergeCodes("2-7-2", "elaborate", {});
  mergeCodes("2-7-3", "explore", {"y2c7s3-explore-main-code": "#define TILT_PIN 2  // Tilt sensor connected to D2\n#define BUZZER_PIN 7 // Buzzer connected to D7\n#define LED_PIN 6   // LED connected to D6\nvoid setup() {\n pinMode(TILT_PIN, INPUT);\n pinMode(BUZZER_PIN, OUTPUT);\n pinMode(LED_PIN, OUTPUT);\n digitalWrite(BUZZER_PIN, LOW);\n digitalWrite(LED_PIN, LOW);\n}\nvoid loop() {\n int tiltState = digitalRead(TILT_PIN);\n if (tiltState == HIGH) {\n  digitalWrite(BUZZER_PIN, HIGH); // Turn buzzer ON\n  digitalWrite(LED_PIN, HIGH);  // Turn LED ON\n } else {\n  digitalWrite(BUZZER_PIN, LOW); // Turn buzzer OFF\n  digitalWrite(LED_PIN, LOW);  // Turn LED OFF\n }\n}", "y2c7s3-explore-try-code": "#define TILT_PIN 2    // Tilt sensor connected to D2\n#define BUZZER_PIN 7  // Buzzer connected to D7\n#define LED1_PIN 6    // First LED on D6\n#define LED2_PIN 5    // Second LED on D5\nvoid setup() {\n  pinMode(TILT_PIN, INPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  digitalWrite(BUZZER_PIN, LOW);\n  digitalWrite(LED1_PIN, LOW);\n  digitalWrite(LED2_PIN, LOW);\n}\nvoid loop() {\n  int tiltState = digitalRead(TILT_PIN);\n  if (tiltState == HIGH) {\n    digitalWrite(BUZZER_PIN, HIGH); // Buzzer ON\n    digitalWrite(LED1_PIN, HIGH);   // LED1 ON\n    digitalWrite(LED2_PIN, HIGH);   // LED2 ON\n  } else {\n    digitalWrite(BUZZER_PIN, LOW);  // Buzzer OFF\n    digitalWrite(LED1_PIN, LOW);    // LED1 OFF\n    digitalWrite(LED2_PIN, LOW);    // LED2 OFF\n  }\n}"});
  mergeCodes("2-7-3", "elaborate", {"y2c7s3-elaborate-project1-code": "// Session 3 Grade 7\n// Taxi Meter\n// Tilt meter down = price starts increasing\n#include <TM1637Display.h>\nconst int tiltSensor = 2;\nconst int buzzer = 7;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int TILT_ACTIVE = HIGH; // change to LOW if opposite\nTM1637Display display(CLK, DIO);\nint price = 0;\nunsigned long lastUpdate = 0;\nvoid setup() {\npinMode(tiltSensor, INPUT);\npinMode(buzzer, OUTPUT);\ndisplay.setBrightness(7);\ndisplay.showNumberDec(0, true);\n}\nvoid loop() {\nif (digitalRead(tiltSensor) == TILT_ACTIVE) {\nif (millis() - lastUpdate > 700) {\nprice = price + 5;\nif (price > 9999) {\nprice = 0;\n}\ndisplay.showNumberDec(price, true);\ndigitalWrite(buzzer, HIGH);\ndelay(100);\ndigitalWrite(buzzer, LOW);\nlastUpdate = millis();\n}\n} else {\nprice = 0;\ndisplay.showNumberDec(0, true);\n}\n}", "y2c7s3-elaborate-project2-code": "// Session 3 Grade 7\n// Taxi Gate / Barrier\n// Tilt down = gate opens\n#include <Servo.h>\nconst int tiltSensor = 2;\nconst int servoPin = 4;\nconst int TILT_ACTIVE = HIGH; // change to LOW if opposite\nServo gateServo;\nvoid setup() {\npinMode(tiltSensor, INPUT);\ngateServo.attach(servoPin);\ngateServo.write(0); // gate closed\n}\nvoid loop() {\nif (digitalRead(tiltSensor) == TILT_ACTIVE) {\ngateServo.write(90); // gate open\n} else {\ngateServo.write(0); // gate closed\n}\n}"});
  mergeCodes("2-7-4", "explore", {"y2c7s4-explore-main-code": "#define HALL_PIN 2 // Hall sensor output pin\n#define LED_PIN1 6 // LED1 pin\n#define LED_PIN2 5 // LED2 pin (opposite behavior)\nvoid setup() {\n pinMode(HALL_PIN, INPUT);\n pinMode(LED_PIN1, OUTPUT);\n pinMode(LED_PIN2, OUTPUT);\n}\nvoid loop() {\n int hallState = digitalRead(HALL_PIN);\n if (hallState == LOW) {     // Magnet detected (output LOW)\n  digitalWrite(LED_PIN1, HIGH); // Turn ON LED at D6\n  digitalWrite(LED_PIN2, LOW); // Turn OFF LED at D5 (opposite)\n } else {\n  digitalWrite(LED_PIN1, LOW); // Turn OFF LED at D6\n  digitalWrite(LED_PIN2, HIGH); // Turn ON LED at D5 (opposite)\n }\n}", "y2c7s4-explore-try-code": "#define HALL_PIN 2   // Hall sensor output pin\n#define LED1_PIN 6   // LED1 (same group)\n#define LED2_PIN 5   // LED2 (opposite group)\n#define LED3_PIN 4   // LED3 (same as LED1)\n#define LED4_PIN 3   // LED4 (same as LED2)\nvoid setup() {\n  pinMode(HALL_PIN, INPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  pinMode(LED3_PIN, OUTPUT);\n  pinMode(LED4_PIN, OUTPUT);\n}\nvoid loop() {\n  int hallState = digitalRead(HALL_PIN);\n  if (hallState == LOW) {  // Magnet detected\n    // Group 1 LEDs ON\n    digitalWrite(LED1_PIN, HIGH);\n    digitalWrite(LED3_PIN, HIGH);\n    // Group 2 LEDs OFF\n    digitalWrite(LED2_PIN, LOW);\n    digitalWrite(LED4_PIN, LOW);\n  } else {\n    // Group 1 LEDs OFF\n    digitalWrite(LED1_PIN, LOW);\n    digitalWrite(LED3_PIN, LOW);\n    // Group 2 LEDs ON\n    digitalWrite(LED2_PIN, HIGH);\n    digitalWrite(LED4_PIN, HIGH);\n  }\n}"});
  mergeCodes("2-7-4", "elaborate", {"y2c7s4-elaborate-project1-code": "// Session 4 Grade 7\n// Washing Machine\n// Plug with magnet near socket sensor = motor\nrotates\n// Magnetic sensor used as DIGITAL sensor on A0\nconst int magneticSensor = A0;\nconst int motor = 5;\nconst int MAGNET_DETECTED = LOW; // change to\nHIGH if opposite\nvoid setup() {\npinMode(magneticSensor, INPUT);\npinMode(motor, OUTPUT);\n}\nvoid loop() {\nif (digitalRead(magneticSensor) ==\nMAGNET_DETECTED) {\ndigitalWrite(motor, HIGH);\n} else {\ndigitalWrite(motor, LOW);\n}\n}", "y2c7s4-elaborate-project2-code": "// Session 4 Grade 7\n// Magnetic Table Lamp\n// Plug with magnet near socket sensor = LED ON\n// Magnetic sensor used as DIGITAL sensor on A0\nconst int magneticSensor = A0;\nconst int led = 6;\nconst int MAGNET_DETECTED = LOW; // change to HIGH if\nopposite\nvoid setup() {\npinMode(magneticSensor, INPUT);\npinMode(led, OUTPUT);\n}\nvoid loop() {\nif (digitalRead(magneticSensor) == MAGNET_DETECTED) {\ndigitalWrite(led, HIGH);\n} else {\ndigitalWrite(led, LOW);\n}\n}"});
  mergeCodes("2-7-5", "explore", {"y2c7s5-explore-main-code": "#define IR_PIN 2  // IR sensor connected to D2\n#define BUZZ_PIN 5 // Buzzer connected to D5\n#define LED_PIN 6 // LED connected to D6 (works opposite to buzzer)\nvoid setup() {\n pinMode(IR_PIN, INPUT);\n pinMode(BUZZ_PIN, OUTPUT);\n pinMode(LED_PIN, OUTPUT);\n digitalWrite(BUZZ_PIN, LOW);\n digitalWrite(LED_PIN, LOW);\n}\nvoid loop() {\n int IRState = digitalRead(IR_PIN);\n if (IRState == LOW) {\n  digitalWrite(BUZZ_PIN, HIGH); // Turn buzzer ON\n  digitalWrite(LED_PIN, LOW); // LED OFF (opposite behavior)\n } else {\n  digitalWrite(BUZZ_PIN, LOW); // Turn buzzer OFF\n  digitalWrite(LED_PIN, HIGH); // LED ON (opposite behavior)\n }\n}", "y2c7s5-explore-try-code": "#define IR_PIN 2     // IR sensor connected to D2\n#define BUZZ1_PIN 5  // Buzzer 1 on D5\n#define BUZZ2_PIN 4  // Buzzer 2 on D4\n#define LED_PIN 6    // LED on D6 (opposite behavior)\nvoid setup() {\n  pinMode(IR_PIN, INPUT);\n  pinMode(BUZZ1_PIN, OUTPUT);\n  pinMode(BUZZ2_PIN, OUTPUT);\n  pinMode(LED_PIN, OUTPUT);\n  digitalWrite(BUZZ1_PIN, LOW);\n  digitalWrite(BUZZ2_PIN, LOW);\n  digitalWrite(LED_PIN, LOW);\n}\nvoid loop() {\n  int IRState = digitalRead(IR_PIN);\n  if (IRState == LOW) {\n    digitalWrite(BUZZ1_PIN, HIGH); // Buzzer 1 ON\n    digitalWrite(BUZZ2_PIN, HIGH); // Buzzer 2 ON\n    digitalWrite(LED_PIN, LOW);    // LED OFF\n  } else {\n    digitalWrite(BUZZ1_PIN, LOW);  // Buzzer 1 OFF\n    digitalWrite(BUZZ2_PIN, LOW);  // Buzzer 2 OFF\n    digitalWrite(LED_PIN, HIGH);   // LED ON\n  }\n}"});
  mergeCodes("2-7-5", "elaborate", {"y2c7s5-elaborate-project1-code": "// Session 5 Grade 7 Project 1\n// Arch Entry Counter\n// Each person detected by IR = count increases on 7 segment\n#include <TM1637Display.h>\nconst int irSensor = 2;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nTM1637Display display(CLK, DIO);\nint countValue = 0;\nbool lastIR = HIGH;\nvoid setup() {\npinMode(irSensor, INPUT);\ndisplay.setBrightness(7);\ndisplay.showNumberDec(0, true);\n}\nvoid loop() {\nbool currentIR = digitalRead(irSensor);\nif (currentIR == IR_DETECTED && lastIR != IR_DETECTED) {\ncountValue++;\nif (countValue > 9999) countValue = 0;\ndisplay.showNumberDec(countValue, true);\ndelay(300);\n}\nlastIR = currentIR;\n}", "y2c7s5-elaborate-project2-code": "// Session 5 Grade 7 Project 2\n// Automatic Servo Gate\n// Person near gate = gate opens for 5 seconds\n#include <Servo.h>\nconst int irSensor = 2;\nconst int servoPin = 3;\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nServo gateServo;\nvoid setup() {\npinMode(irSensor, INPUT);\ngateServo.attach(servoPin);\ngateServo.write(0); // gate closed\n}\nvoid loop() {\nif (digitalRead(irSensor) == IR_DETECTED) {\ngateServo.write(90); // gate open\ndelay(5000);\ngateServo.write(0); // gate closed\ndelay(500);\n}\n}"});
  mergeCodes("2-7-6", "explore", {"y2c7s6-explore-main-code": "#include <Servo.h>\n#define SERVO1_PIN 2   // First Servo on D2\n#define SERVO2_PIN 3   // Second Servo on D3\nServo servo1;\nServo servo2;\nvoid setup() {\n  servo1.attach(SERVO1_PIN);\n  servo2.attach(SERVO2_PIN);\n}\nvoid loop() {\n  // 0 → 180\n  for (int angle = 0; angle <= 180; angle++) {\n    servo1.write(angle);\n    servo2.write(angle);\n    delay(15);\n  }\n  // 180 → 0\n  for (int angle = 180; angle >= 0; angle--) {\n    servo1.write(angle);\n    servo2.write(angle);\n    delay(15);\n  }\n}", "y2c7s6-explore-try-code": "#include <Servo.h>\n#define SERVO1_PIN 2\n#define SERVO2_PIN 3\nServo servo1;\nServo servo2;\nvoid setup() {\n  servo1.attach(SERVO1_PIN);\n  servo2.attach(SERVO2_PIN);\n}\nvoid loop() {\n  // 0 → 90\n  for (int angle = 0; angle <= 90; angle++) {\n    servo1.write(angle);\n    servo2.write(angle);\n    delay(15);\n  }\n  // 90 → 0\n  for (int angle = 90; angle >= 0; angle--) {\n    servo1.write(angle);\n    servo2.write(angle);\n    delay(15);\n  }\n}"});
  mergeCodes("2-7-6", "elaborate", {"y2c7s6-elaborate-project1-code": "// Session 6 Grade 7 Project 1\n// Automatic Door\n// IR detected = door opens for 5 seconds and closes\n#include <Servo.h>\nconst int irSensor = 2;\nconst int servoPin = 3;\nconst int IR_DETECTED = LOW ;   // change to HIGH if opposite\nServo doorServo;\nvoid setup() {\npinMode(irSensor , INPUT);\ndoorServo.attach(servoPin);\ndoorServo.write(0);          // door closed\n}\nvoid loop() {\nif (digitalRead(irSensor) == IR_DETECTED) {\ndoorServo.write(90);       // door open\ndelay(5000);\ndoorServo.write(0);        // door close\ndelay(500);\n}\n}", "y2c7s6-elaborate-project2-code": "// Session 6 Grade 7 Project 2\n// Robo Hand Hi\n// Touch detected = robot waves hand\n#include <Servo.h>\nconst int TOUCH_PIN = 2;\nconst int SERVO_PIN = 3;\nServo myServo;\nvoid setup() {\npinMode(TOUCH_PIN, INPUT);\nmyServo.attach(SERVO_PIN);\n\nmyServo.write(0); // Hand starts down/at rest\n}\nvoid loop() {\nint touchState = digitalRead(TOUCH_PIN);\nif (touchState == HIGH) {\n\n// The Waving Sequence (2 quick shakes)\nfor (int i = 0; i < 2; i++) {\nmyServo.write(75); // Move up to wave (75° keeps it a bit tighter/faster than\n90°)\ndelay(200); // Quick pause at the top\n\nmyServo.write(15); // Move back down\ndelay(200); // Quick pause at the bottom\n}\n\nmyServo.write(0); // Put the hand back to rest position\ndelay(500); // Cooldown so it doesn't immediately wave again if you linger\nyour finger\n}\n}"});
  mergeCodes("2-7-7", "explore", {"y2c7s7-explore-main-code": "#include <TM1637Display.h> // Include library for 4-digit 7-segment display\n#define CLK 8 // Clock pin connected to D8\n#define DIO 9 // Data pin connected to D9\n#define BUTTON_PIN 5 // Push button connected to D5\nTM1637Display display(CLK, DIO); // Create display object\nint count = 0;     // Variable to store button press count\nint lastButtonState = LOW;\nvoid setup() {\n pinMode(BUTTON_PIN, INPUT);  // Set button pin as input\n display.setBrightness(7);   // Set display brightness (0 to 7)\n display.showNumberDec(0);   // Initialize display with 0\n}\nvoid loop() {\n int buttonState = digitalRead(BUTTON_PIN);\n // Detect button press (LOW to HIGH transition)\n if (buttonState == HIGH && lastButtonState == LOW) {\n  count++;           // Increment count\n  if (count > 9999) count = 0; // Reset after 9999\n  display.showNumberDec(count); // Display the count\n  delay(200);         // Debounce delay\n }\n lastButtonState = buttonState; // Store last button state\n}", "y2c7s7-explore-try-code": "#include <TM1637Display.h>\n#define CLK 8\n#define DIO 9\n#define BUTTON_PIN 5\nTM1637Display display(CLK, DIO);\nint count = 0;\nbool running = false;   // Start/Stop flag\nint lastButtonState = LOW;\nunsigned long lastPressTime = 0;\nint pressCount = 0;\nvoid setup() {\n  pinMode(BUTTON_PIN, INPUT);\n  display.setBrightness(7);\n  display.showNumberDec(0);\n}\nvoid loop() {\n  int buttonState = digitalRead(BUTTON_PIN);\n  // Detect button press\n  if (buttonState == HIGH && lastButtonState == LOW) {\n    pressCount++;\n    lastPressTime = millis();\n    delay(50); // debounce\n  }\n\n // Check for double press within 400ms\n if (pressCount == 2 && (millis() - lastPressTime) < 400) {\n running = !running; // Toggle start/stop\n pressCount = 0;\n delay(200);\n }\n // Reset if too slow (not a double press)\n if ((millis() - lastPressTime) > 400) {\n pressCount = 0;\n }\n // Counting logic\n if (running) {\n display.showNumberDec(count);\n count++;\n if (count > 9999) count = 0;\n delay(500);\n }\n lastButtonState = buttonState;\n}"});
  mergeCodes("2-7-7", "elaborate", {"y2c7s7-elaborate-project1-code": "// Session 7 Grade 7 Project 2\n// Petrol Bunk Meter\n// Hold touch sensor = petrol value increases on 7 segment\n#include <TM1637Display.h>\nconst int touchSensor = 2;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int TOUCH_ACTIVE = HIGH; // change to LOW if opposite\nTM1637Display display(CLK, DIO);\nint petrolValue = 0;\nunsigned long lastUpdate = 0;\nvoid setup() {\npinMode(touchSensor, INPUT);\ndisplay.setBrightness(7);\ndisplay.showNumberDec(0, true);\n}\nvoid loop() {\nif (digitalRead(touchSensor) == TOUCH_ACTIVE) {\nif (millis() - lastUpdate > 300) {\npetrolValue++;\nif (petrolValue > 9999) petrolValue = 0;\ndisplay.showNumberDec(petrolValue, true);\nlastUpdate = millis();\n}\n}\n}"});
  mergeCodes("2-7-8", "explore", {"y2c7s8-explore-main-code": "#define TOUCH_PIN 2 // Touch sensor connected to D2\n#define LED_PIN 6  // LED connected to D6\nvoid setup() {\n pinMode(TOUCH_PIN, INPUT);\n pinMode(LED_PIN, OUTPUT);\n}\nvoid loop() {\n int touchState = digitalRead(TOUCH_PIN);\n if (touchState == HIGH) {     // When touch sensor is pressed\n  for (int brightness = 0; brightness <= 255; brightness++) {\n   analogWrite(LED_PIN, brightness); // Gradually increase LED brightness\n   delay(5);            // Smooth fade effect\n  }\n } else {\n  analogWrite(LED_PIN, 0);      // Turn LED OFF when not touched\n }\n}", "y2c7s8-explore-try-code": "#define TOUCH_PIN 2   // Touch sensor on D2\n#define LED1_PIN 6    // LED1 on D6\n#define LED2_PIN 5    // LED2 on D5\n#define LED3_PIN 3    // LED3 on D3\nvoid setup() {\n  pinMode(TOUCH_PIN, INPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  pinMode(LED3_PIN, OUTPUT);\n}\nvoid loop() {\n  int touchState = digitalRead(TOUCH_PIN);\n  if (touchState == HIGH) {\n    // Fade all LEDs together\n    for (int brightness = 0; brightness <= 255; brightness++) {\n      analogWrite(LED1_PIN, brightness);\n      analogWrite(LED2_PIN, brightness);\n      analogWrite(LED3_PIN, brightness);\n      delay(5);\n    }\n  } else {\n    // Turn all OFF\n    analogWrite(LED1_PIN, 0);\n    analogWrite(LED2_PIN, 0);\n    analogWrite(LED3_PIN, 0);\n  }\n}"});
  mergeCodes("2-7-8", "elaborate", {"y2c7s8-elaborate-project1-code": "// Session 8 Grade 7\n// Dancing Robot\n// IR detects person = RGB and 7 segment dance pattern starts\n// Push button turns it OFF\n// D12 is kept always HIGH for RGB project\n#include <TM1637Display.h>\nconst int irSensor = 2;\nconst int pushButton = 5;\nconst int redPin = 10;\nconst int greenPin = 11;\nconst int bluePin = 12;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int IR_DETECTED = LOW;   // change to HIGH if opposite\nTM1637Display display(CLK, DIO);\nbool danceOff = false;\nint patternNumber = 0;\nvoid setup() {\npinMode(irSensor, INPUT);\npinMode(pushButton, INPUT_PULLUP);\npinMode(redPin, OUTPUT);\npinMode(greenPin, OUTPUT);\npinMode(bluePin, OUTPUT);\ndigitalWrite(bluePin, HIGH); // D12 always HIGH\ndisplay.setBrightness(7);\ndisplay.showNumberDec(0, true);\n}\nvoid loop() {\ndigitalWrite(bluePin, HIGH); // keep D12 always HIGH\nif (digitalRead(pushButton) == LOW) {\ndanceOff = true;\nallOffExceptD12();\ndisplay.showNumberDec(0, true);\ndelay(300);\n}\nif (digitalRead(irSensor) == IR_DETECTED) {\ndanceOff = false;\n}\nif (digitalRead(irSensor) == IR_DETECTED && !danceOff) {\ndancePattern();\n} else {\nallOffExceptD12();\n}\n}\nvoid dancePattern() {\npatternNumber++;\nif (patternNumber > 9) patternNumber = 1;\ndisplay.showNumberDec(patternNumber, true);\ndigitalWrite(redPin, HIGH);\ndigitalWrite(greenPin, LOW);\ndigitalWrite(bluePin, HIGH);\ndelay(200);\ndigitalWrite(redPin, LOW);\ndigitalWrite(greenPin, HIGH);\ndigitalWrite(bluePin, HIGH);\ndelay(200);\ndigitalWrite(redPin, HIGH);\ndigitalWrite(greenPin, HIGH);\ndigitalWrite(bluePin, HIGH);\ndelay(200);\n}\nvoid allOffExceptD12() {\ndigitalWrite(redPin, LOW);\ndigitalWrite(greenPin, LOW);\ndigitalWrite(bluePin, HIGH);\n}"});
  mergeCodes("2-7-9", "explore", {"y2c7s9-explore-main-code": "#define LASER_PIN 2 // Laser diode connected to D2\n#define LDR_PIN 3  // LDR sensor connected to D3\n#define BUZZER_PIN 5 // Buzzer connected to D5\nvoid setup() {\n pinMode(LASER_PIN, OUTPUT);\n pinMode(LDR_PIN, INPUT);\n pinMode(BUZZER_PIN, OUTPUT);\n digitalWrite(LASER_PIN, HIGH); // Turn ON laser initially\n digitalWrite(BUZZER_PIN, LOW); // Keep buzzer OFF\n}\nvoid loop() {\n int ldrState = digitalRead(LDR_PIN); // Read LDR state\n if (ldrState == HIGH) {       // Laser beam detected normally\n  digitalWrite(BUZZER_PIN, LOW);   // Buzzer OFF\n } else {               // Laser beam blocked\n  digitalWrite(BUZZER_PIN, HIGH);  // Buzzer ON (Alarm)\n }\n}", "y2c7s9-explore-try-code": "#define LASER_PIN 2   // Laser diode on D2\n#define LDR_PIN 3     // LDR sensor on D3\n#define LED_PIN 5     // LED on D5\nvoid setup() {\n  pinMode(LASER_PIN, OUTPUT);\n  pinMode(LDR_PIN, INPUT);\n  pinMode(LED_PIN, OUTPUT);\n  digitalWrite(LASER_PIN, HIGH); // Laser ON\n  digitalWrite(LED_PIN, LOW);    // LED OFF initially\n}\nvoid loop() {\n  int ldrState = digitalRead(LDR_PIN);\n  if (ldrState == HIGH) {\n    digitalWrite(LED_PIN, LOW);   // Beam normal → LED OFF\n  } else {\n    digitalWrite(LED_PIN, HIGH);  // Beam broken → LED ON\n  }\n}"});
  mergeCodes("2-7-9", "elaborate", {});
  mergeCodes("2-8-1", "explore", {"y2c8s1-explore-main-code": "#define LDR_PIN 2     // LDR sensor on D2\n#define BUZZ1_PIN 6   // Buzzer 1 on D6\n#define BUZZ2_PIN 7   // Buzzer 2 on D7 (opposite)\nvoid setup() {\n  pinMode(LDR_PIN, INPUT);\n  pinMode(BUZZ1_PIN, OUTPUT);\n  pinMode(BUZZ2_PIN, OUTPUT);\n  digitalWrite(BUZZ1_PIN, LOW);\n  digitalWrite(BUZZ2_PIN, LOW);\n}\nvoid loop() {\n  int ldrstate = digitalRead(LDR_PIN);\n  if (ldrstate == HIGH) {\n    digitalWrite(BUZZ1_PIN, HIGH); // Buzzer1 ON\n    digitalWrite(BUZZ2_PIN, LOW);  // Buzzer2 OFF\n  } else {\n    digitalWrite(BUZZ1_PIN, LOW);  // Buzzer1 OFF\n    digitalWrite(BUZZ2_PIN, HIGH); // Buzzer2 ON\n  }\n}", "y2c8s1-explore-try-code": "#define LDR_PIN 2     // LDR sensor on D2\n#define BUZZER_PIN 6  // Buzzer on D6\nvoid setup() {\n  pinMode(LDR_PIN, INPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  digitalWrite(BUZZER_PIN, LOW);\n}\nvoid loop() {\n  int ldrstate = digitalRead(LDR_PIN);\n  if (ldrstate == HIGH) {\n    digitalWrite(BUZZER_PIN, HIGH); // Bright → Buzzer ON\n  } else {\n    digitalWrite(BUZZER_PIN, LOW);  // Dark → Buzzer OFF\n  }\n}"});
  mergeCodes("2-8-1", "elaborate", {"y2c8s1-elaborate-project1-code": "// Session 1 Grade 8\n// Lamp - LDR Auto Yellow LED\nconst int ldrPin = A0;\nconst int yellowLED = 6;\nint darkValue = 500; // adjust after testing\nvoid setup() {\npinMode(yellowLED, OUTPUT);\n}\nvoid loop() {\nint ldrValue = analogRead(ldrPin);\nif (ldrValue > darkValue) {\ndigitalWrite(yellowLED, HIGH);\n} else {\ndigitalWrite(yellowLED, LOW);\n}\n}", "y2c8s1-elaborate-project2-code": "// Session 1 Grade 8\n// Lamp - LDR Auto Yellow LED\nconst int ldrPin = A0;\nconst int yellowLED = 6;\nint darkValue = 500; // adjust after testing\nvoid setup() {\npinMode(yellowLED, OUTPUT);\n}\nvoid loop() {\nint ldrValue = analogRead(ldrPin);\nif (ldrValue > darkValue) {\ndigitalWrite(yellowLED, HIGH);\n} else {\ndigitalWrite(yellowLED, LOW);\n}\n}"});
  mergeCodes("2-8-10", "explore", {"y2c8s10-explore-main-code": "#define SOUND_PIN A3 // Analog output pin from sound sensor\n#define LED_PIN 5   // LED connected to D5\nvoid setup() {\n Serial.begin(9600); // Start the Serial Monitor\n pinMode(LED_PIN, OUTPUT);\n}\nvoid loop() {\n int soundValue = analogRead(SOUND_PIN); // Read the sound sensor value (0-1023)\n Serial.print(\"Sound value: \");\n Serial.println(soundValue);      // Print the sound value\n int brightness = map(soundValue, 0, 1023, 0, 255); // Map to PWM range\n analogWrite(LED_PIN, brightness);         // Adjust LED brightness\n delay(100); // Small delay to smooth the readings\n}", "y2c8s10-explore-try-code": "#define SOUND_PIN A3   // Sound sensor on A3\n#define BUZZER_PIN 5   // Buzzer on D5\nvoid setup() {\n  Serial.begin(9600);\n  pinMode(BUZZER_PIN, OUTPUT);\n}\nvoid loop() {\n  int soundValue = analogRead(SOUND_PIN); // Read sound (0–1023)\n  Serial.print(\"Sound value: \");\n  Serial.println(soundValue);\n  // Trigger buzzer if sound is loud\n  if (soundValue > 500) {\n    digitalWrite(BUZZER_PIN, HIGH); // Buzzer ON\n  } else {\n    digitalWrite(BUZZER_PIN, LOW);  // Buzzer OFF\n  }\n  delay(100);\n}"});
  mergeCodes("2-8-10", "elaborate", {"y2c8s10-elaborate-project1-code": "// Session 10 Grade 8\n// Classroom Noise Meter + Emergency System\n// Sound sensor value shown on 7 segment\n// High noise = buzzer warning\n// Touch sensor = emergency buzzer pattern\n#include <TM1637Display.h>\nconst int soundSensor = A0;\nconst int touchSensor = 2;\nconst int buzzer = 7;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int TOUCH_ACTIVE = HIGH; // change to LOW if opposite\nint noiseLimit = 700;\nTM1637Display display(CLK, DIO);\nvoid setup() {\npinMode(touchSensor, INPUT);\npinMode(buzzer, OUTPUT);\ndisplay.setBrightness(7);\ndisplay.showNumberDec(0, true);\n}\nvoid loop() {\nint soundValue = analogRead(soundSensor);\nint noiseLevel = map(soundValue, 0, 1023, 0, 9999);\ndisplay.showNumberDec(noiseLevel, true);\n\nif (digitalRead(touchSensor) == TOUCH_ACTIVE) {\nemergencyAlarm();\n} else if (soundValue > noiseLimit) {\ndigitalWrite(buzzer, HIGH);\ndelay(100);\ndigitalWrite(buzzer, LOW);\ndelay(100);\n} else {\ndigitalWrite(buzzer, LOW);\n}\n}\nvoid emergencyAlarm() {\nfor (int i = 0; i < 5; i++) {\ndigitalWrite(buzzer, HIGH);\ndelay(150);\ndigitalWrite(buzzer, LOW);\ndelay(150);\n}\n}"});
  mergeCodes("2-8-2", "explore", {"y2c8s2-explore-main-code": "#define SOUND_PIN A3  // Sound sensor on A3\n#define LED1_PIN 5    // LED1 on D5\n#define LED2_PIN 6    // LED2 on D6\n#define LED3_PIN 3    // LED3 on D3\nvoid setup() {\n  Serial.begin(9600);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  pinMode(LED3_PIN, OUTPUT);\n}\nvoid loop() {\n  int soundValue = analogRead(SOUND_PIN);\n  Serial.print(\"Sound value: \");\n  Serial.println(soundValue);\n  int brightness = map(soundValue, 0, 1023, 0, 255);\n  // Apply same brightness to all LEDs\n  analogWrite(LED1_PIN, brightness);\n  analogWrite(LED2_PIN, brightness);\n  analogWrite(LED3_PIN, brightness);\n  delay(100);\n}", "y2c8s2-explore-try-code": "#define SOUND_PIN A3   // Analog output pin from sound sensor\n#define LED_PIN 6      // LED moved to D6 (PWM pin)\n#define BUZZER_PIN 5   // Buzzer connected to D5\nvoid setup() {\n  Serial.begin(9600);\n  pinMode(LED_PIN, OUTPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n}\nvoid loop() {\n  int soundValue = analogRead(SOUND_PIN); // Read sound (0–1023)\n  Serial.print(\"Sound value: \");\n  Serial.println(soundValue);\n  int brightness = map(soundValue, 0, 1023, 0, 255);\n  analogWrite(LED_PIN, brightness); // LED brightness based on sound\n  // Buzzer logic (turn ON if sound is loud)\n  if (soundValue > 500) {\n    digitalWrite(BUZZER_PIN, HIGH); // Buzzer ON\n  } else {\n    digitalWrite(BUZZER_PIN, LOW);  // Buzzer OFF\n  }\n  delay(100);\n}"});
  mergeCodes("2-8-2", "elaborate", {"y2c8s2-elaborate-project1-code": "#include <Servo.h>\nServo doorServo;\nint irSensor = 2;\nint servoPin = 3;\nint IR_ACTIVE = LOW; // Change to HIGH if opposite\nvoid setup() {\npinMode(irSensor, INPUT);\ndoorServo.attach(servoPin);\ndoorServo.write(130); // Door closed\n}\nvoid loop() {\nif (digitalRead(irSensor) == IR_ACTIVE) {\ndelay(1000); // IR detected for 5 seconds\n// Open door smoothly\nfor (int i = 130; i >= 30; i--) {\ndoorServo.write(i);\ndelay(40);\n\n}\ndelay(5000); // Door open for 5 seconds\nfor (int i = 30; i <= 130; i++) {\ndoorServo.write(i);\ndelay(25);\n// Close door smoothly\n\n}\n}\n}", "y2c8s2-elaborate-project2-code": "// Session 2 Grade 8\n// Body Builder\n// Sound detected = Servo moves arm like bicep movement\n#include <Servo.h>\nconst int soundSensor = A0;\nconst int servoPin = 4;\nint soundLimit = 600; // adjust after testing\nServo bodyServo;\nvoid setup() {\nbodyServo.attach(servoPin);\nbodyServo.write(0);\n}\nvoid loop() {\nint soundValue = analogRead(soundSensor);\nif (soundValue > soundLimit) {\nfor (int i = 0; i < 3; i++) {\nbodyServo.write(90);\ndelay(400);\nbodyServo.write(0);\ndelay(400);\n}\n}\n}"});
  mergeCodes("2-8-3", "explore", {"y2c8s3-explore-main-code": "#define TILT_PIN 2   // Tilt sensor on D2\n#define LED1_PIN 6   // LED1 on D6\n#define LED2_PIN 5   // LED2 on D5\nvoid setup() {\n  pinMode(TILT_PIN, INPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  digitalWrite(LED1_PIN, LOW);\n  digitalWrite(LED2_PIN, LOW);\n}\nvoid loop() {\n  int tiltState = digitalRead(TILT_PIN);\n  if (tiltState == HIGH) {\n    digitalWrite(LED1_PIN, HIGH); // LED1 ON\n    digitalWrite(LED2_PIN, HIGH); // LED2 ON\n  } else {\n    digitalWrite(LED1_PIN, LOW);  // LED1 OFF\n    digitalWrite(LED2_PIN, LOW);  // LED2 OFF\n  }\n}", "y2c8s3-explore-try-code": "#define TILT_PIN 2    // Tilt sensor connected to D2\n#define BUZZER_PIN 7  // Buzzer connected to D7\n#define LED1_PIN 6    // First LED on D6\n#define LED2_PIN 5    // Second LED on D5\nvoid setup() {\n  pinMode(TILT_PIN, INPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  digitalWrite(BUZZER_PIN, LOW);\n  digitalWrite(LED1_PIN, LOW);\n  digitalWrite(LED2_PIN, LOW);\n}\nvoid loop() {\n  int tiltState = digitalRead(TILT_PIN);\n  if (tiltState == HIGH) {\n    digitalWrite(BUZZER_PIN, HIGH); // Buzzer ON\n    digitalWrite(LED1_PIN, HIGH);   // LED1 ON\n    digitalWrite(LED2_PIN, HIGH);   // LED2 ON\n  } else {\n    digitalWrite(BUZZER_PIN, LOW);  // Buzzer OFF\n    digitalWrite(LED1_PIN, LOW);    // LED1 OFF\n    digitalWrite(LED2_PIN, LOW);    // LED2 OFF\n  }\n}"});
  mergeCodes("2-8-3", "elaborate", {"y2c8s3-elaborate-project1-code": "// Session 3 Grade 8\n// Railway Track\n// Tilt down = track moves into correct position\n#include <Servo.h>\nconst int tiltSensor = 2;\nconst int servoPin = 4;\nconst int TILT_ACTIVE = HIGH; // change to LOW if\nopposite\nServo trackServo;\nvoid setup() {\npinMode(tiltSensor, INPUT);\ntrackServo.attach(servoPin);\ntrackServo.write(0); // track open / wrong position\n}\nvoid loop() {\nif (digitalRead(tiltSensor) == TILT_ACTIVE) {\ntrackServo.write(90); // track correct position\n} else {\ntrackServo.write(0);\n}\n}", "y2c8s3-elaborate-project2-code": "//Session 3 Grade 8\n// Counter\n// Every tilt = count increases by 1\n#include <TM1637Display.h>\nconst int tiltSensor = 2;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int TILT_ACTIVE = HIGH; // change to LOW if opposite\nTM1637Display display(CLK, DIO);\nint countValue = 0;\nbool lastTiltState = LOW;\nvoid setup() {\npinMode(tiltSensor, INPUT);\ndisplay.setBrightness(7);\ndisplay.showNumberDec(0, true);\n}\nvoid loop() {\nbool currentTilt = digitalRead(tiltSensor);\nif (currentTilt == TILT_ACTIVE && lastTiltState != TILT_ACTIVE) {\ncountValue++;\nif (countValue > 9999) {\ncountValue = 0;\n}\ndisplay.showNumberDec(countValue, true);\ndelay(300);\n}\nlastTiltState = currentTilt;\n}"});
  mergeCodes("2-8-4", "explore", {"y2c8s4-explore-main-code": "// Hall Sensor with Buzzer and LED (opposite behavior)\n// Board: Arduino UNO (or compatible)\n#define HALL_PIN 2  // Hall sensor output pin\n#define BUZZ_PIN 6  // Buzzer pin (active on detection)\n#define LED_PIN 5  // LED pin (opposite behavior)\nvoid setup() {\n pinMode(HALL_PIN, INPUT);\n pinMode(BUZZ_PIN, OUTPUT);\n pinMode(LED_PIN, OUTPUT);\n digitalWrite(BUZZ_PIN, LOW);\n digitalWrite(LED_PIN, HIGH); // Default: LED ON when no magnet\n}\nvoid loop() {\n int hallState = digitalRead(HALL_PIN);\n if (hallState == LOW) {     // Magnet detected (output LOW)\n  digitalWrite(BUZZ_PIN, HIGH); // Turn ON buzzer\n  digitalWrite(LED_PIN, LOW);  // Turn OFF LED (opposite)\n } else {\n  digitalWrite(BUZZ_PIN, LOW); // Turn OFF buzzer\n  digitalWrite(LED_PIN, HIGH); // Turn ON LED (opposite)\n }\n}", "y2c8s4-explore-try-code": "#define HALL_PIN 2    // Hall sensor output pin\n#define BUZZ1_PIN 6   // Buzzer 1\n#define BUZZ2_PIN 7   // Buzzer 2\nvoid setup() {\n  pinMode(HALL_PIN, INPUT);\n  pinMode(BUZZ1_PIN, OUTPUT);\n  pinMode(BUZZ2_PIN, OUTPUT);\n  digitalWrite(BUZZ1_PIN, LOW);\n  digitalWrite(BUZZ2_PIN, LOW);\n}\nvoid loop() {\n  int hallState = digitalRead(HALL_PIN);\n  if (hallState == LOW) {  // Magnet detected\n    digitalWrite(BUZZ1_PIN, HIGH); // Buzzer 1 ON\n    digitalWrite(BUZZ2_PIN, HIGH); // Buzzer 2 ON\n  } else {\n    digitalWrite(BUZZ1_PIN, LOW);  // Buzzer 1 OFF\n    digitalWrite(BUZZ2_PIN, LOW);  // Buzzer 2 OFF\n  }\n}"});
  mergeCodes("2-8-4", "elaborate", {"y2c8s4-elaborate-project1-code": "// Session 4 Grade 8\n// Helmet Bike\n// Helmet magnet detected = bike wheel motor ON\n// Magnetic sensor used as DIGITAL sensor on A0\nconst int magneticSensor = A0;\nconst int motor = 5;\nconst int MAGNET_DETECTED = LOW; // change to\nHIGH if opposite\nvoid setup() {\npinMode(magneticSensor, INPUT);\npinMode(motor, OUTPUT);\n}\nvoid loop() {\nif (digitalRead(magneticSensor) ==\nMAGNET_DETECTED) {\ndigitalWrite(motor, HIGH);\n} else {\ndigitalWrite(motor, LOW);\n}\n}", "y2c8s4-elaborate-project2-code": "// Session 4 Grade 8\n// Seatbelt / Headlight\n// Magnet detected = car headlight ON\n// RGB LED project: D12 is always HIGH\n// Magnetic sensor used as DIGITAL sensor on A0\nconst int magneticSensor = A0;\nconst int redPin = 10;\nconst int greenPin = 11;\nconst int alwaysHighPin = 12;\nconst int MAGNET_DETECTED = LOW; // change to HIGH if opposite\nvoid setup() {\npinMode(magneticSensor, INPUT);\npinMode(redPin, OUTPUT);\npinMode(greenPin, OUTPUT);\npinMode(alwaysHighPin, OUTPUT);\ndigitalWrite(alwaysHighPin, HIGH); // D12 always HIGH\n}\nvoid loop() {\ndigitalWrite(alwaysHighPin, HIGH); // keep D12 always HIGH\nif (digitalRead(magneticSensor) == MAGNET_DETECTED) {\ndigitalWrite(redPin, HIGH);\ndigitalWrite(greenPin, HIGH);\n} else {\ndigitalWrite(redPin, LOW);\ndigitalWrite(greenPin, LOW);\n}\n}"});
  mergeCodes("2-8-5", "explore", {"y2c8s5-explore-main-code": "// IR LED Detector using two LEDs on D5 (detected) and D6 (idle)\n// Board: Arduino UNO (or compatible)\n#define IR_PIN 2     // IR sensor connected to D2\n#define LED_DETECTED 5  // LED that turns ON when something is detected\n#define LED_IDLE 6    // LED that stays ON when nothing is detected\nvoid setup() {\n pinMode(IR_PIN, INPUT);    // Use INPUT for modules with onboard comparator\n pinMode(LED_DETECTED, OUTPUT);\n pinMode(LED_IDLE, OUTPUT);\n // Initial states: assume no detection\n digitalWrite(LED_DETECTED, LOW);\n digitalWrite(LED_IDLE, HIGH);\n}\nvoid loop() {\n int irState = digitalRead(IR_PIN);\n // Many IR obstacle sensors pull the output LOW when an object is detected.\n // If your module behaves opposite, swap HIGH/LOW in the condition below.\n bool detected = (irState == LOW);\n if (detected) {\n  digitalWrite(LED_DETECTED, HIGH); // Turn ON D5 LED (detected)\n  digitalWrite(LED_IDLE, LOW);    // Turn OFF D6 LED\n } else {\n  digitalWrite(LED_DETECTED, LOW);  // Turn OFF D5 LED\n  digitalWrite(LED_IDLE, HIGH);   // Turn ON D6 LED (idle/clear)\n }\n}", "y2c8s5-explore-try-code": "#define IR_PIN 2          // IR sensor connected to D2\n#define LED_DETECTED 5    // LED for detection\n#define LED_IDLE 6        // LED for idle state\n#define LED_EXTRA 7       // Extra LED (same as detected)\nvoid setup() {\n  pinMode(IR_PIN, INPUT);\n  pinMode(LED_DETECTED, OUTPUT);\n  pinMode(LED_IDLE, OUTPUT);\n  pinMode(LED_EXTRA, OUTPUT);\n  // Initial state\n  digitalWrite(LED_DETECTED, LOW);\n  digitalWrite(LED_IDLE, HIGH);\n  digitalWrite(LED_EXTRA, LOW);\n}\nvoid loop() {\n  int irState = digitalRead(IR_PIN);\n  bool detected = (irState == LOW);\n  if (detected) {\n    digitalWrite(LED_DETECTED, HIGH); // Detected LED ON\n    digitalWrite(LED_EXTRA, HIGH);    // Extra LED ON\n    digitalWrite(LED_IDLE, LOW);      // Idle LED OFF\n  } else {\n    digitalWrite(LED_DETECTED, LOW);  // Detected LED OFF\n    digitalWrite(LED_EXTRA, LOW);     // Extra LED OFF\n    digitalWrite(LED_IDLE, HIGH);     // Idle LED ON\n  }\n}"});
  mergeCodes("2-8-5", "elaborate", {"y2c8s5-elaborate-project1-code": "// Session 5 Grade 8 Project 1\n// Crocodile Mouth\n// Hand near crocodile = mouth opens and closes\n#include <Servo.h>\nconst int irSensor = 2;\nconst int servoPin = 4;\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nServo mouthServo;\nvoid setup() {\npinMode(irSensor, INPUT);\nmouthServo.attach(servoPin);\nmouthServo.write(0); // mouth closed\n}\nvoid loop() {\nif (digitalRead(irSensor) == IR_DETECTED) {\nmouthServo.write(80); // mouth open\ndelay(1000);\nmouthServo.write(0); // mouth closed\ndelay(500);\n}\n}", "y2c8s5-elaborate-project2-code": "// Session 5 Grade 8 Project 2\n// Panda Face Movement\n// Person near panda = face tilts\n#include <Servo.h>\nconst int irSensor = 2;\nconst int servoPin = 3;\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nServo faceServo;\nvoid setup() {\npinMode(irSensor, INPUT);\nfaceServo.attach(servoPin);\nfaceServo.write(90); // center position\n}\nvoid loop() {\nif (digitalRead(irSensor) == IR_DETECTED) {\nfaceServo.write(50);\ndelay(500);\nfaceServo.write(130);\ndelay(500);\nfaceServo.write(90);\ndelay(500);\n}\n}"});
  mergeCodes("2-8-6", "explore", {"y2c8s6-explore-main-code": "#include <Servo.h>\n#define TOUCH_PIN 2   // Touch sensor on D2\n#define SERVO_PIN 3   // Servo on D3\nServo myServo;\nvoid setup() {\n  pinMode(TOUCH_PIN, INPUT);\n  myServo.attach(SERVO_PIN);\n  myServo.write(0); // Start at 0°\n}\nvoid loop() {\n  int touchState = digitalRead(TOUCH_PIN);\n  if (touchState == HIGH) {  // Touch detected\n    // Move from 0 → 180\n    for (int angle = 0; angle <= 180; angle++) {\n      myServo.write(angle);\n      delay(10);\n    }\n  } else {\n    myServo.write(0); // Return to 0°\n  }\n}", "y2c8s6-explore-try-code": "#include <Servo.h>\n#define TOUCH_PIN 2    // Touch sensor on D2\n#define SERVO_PIN 3    // Servo on D3\n#define BUZZER_PIN 5   // Buzzer on D5\nServo myServo;\nvoid setup() {\n  pinMode(TOUCH_PIN, INPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  myServo.attach(SERVO_PIN);\n  myServo.write(0); // Start at 0°\n  digitalWrite(BUZZER_PIN, LOW);\n}\nvoid loop() {\n  int touchState = digitalRead(TOUCH_PIN);\n  if (touchState == HIGH) {  // Touch detected\n    digitalWrite(BUZZER_PIN, HIGH); // Buzzer ON\n    // Move from 0 → 180\n    for (int angle = 0; angle <= 180; angle++) {\n      myServo.write(angle);\n      delay(10);\n    }\n  } else {\n    digitalWrite(BUZZER_PIN, LOW); // Buzzer OFF\n    myServo.write(0);              // Return to 0°\n  }\n}\n}"});
  mergeCodes("2-8-6", "elaborate", {"y2c8s6-elaborate-project1-code": "// Session 6 Grade 8 Project 1\n// Crane Movement\n// Touch sensor touched = crane arm goes down and up\n#include <Servo.h>\nconst int touchSensor = 2;\nconst int servoPin = 3;\nconst int TOUCH_ACTIVE = HIGH; // change to LOW if opposite\nServo craneServo;\nvoid setup() {\npinMode(touchSensor, INPUT);\ncraneServo.attach(servoPin);\ncraneServo.write(0); // crane up\n}\nvoid loop() {\nif (digitalRead(touchSensor) == TOUCH_ACTIVE) {\ncraneServo.write(100); // crane down\ndelay(1000);\ncraneServo.write(0); // crane up\ndelay(500);\n}\n}", "y2c8s6-elaborate-project2-code": "// Session 6 Grade 8 Project 2\n// Robo Hi With Touch\n// Touch sensor touched = robot waves hand\n#include <Servo.h>\nconst int touchSensor = 2;\nconst int servoPin = 3;\nconst int TOUCH_ACTIVE = HIGH; // change to LOW if opposite\nServo handServo;\nvoid setup() {\npinMode(touchSensor, INPUT);\nhandServo.attach(servoPin);\nhandServo.write(90);\n}\nvoid loop() {\nif (digitalRead(touchSensor) == TOUCH_ACTIVE) {\nfor (int i = 0; i < 3; i++) {\nhandServo.write(30);\ndelay(400);\nhandServo.write(120);\ndelay(400);\n}\nhandServo.write(90);\ndelay(500);\n}\n}"});
  mergeCodes("2-8-7", "explore", {"y2c8s7-explore-main-code": "#include <TM1637Display.h> // Include library for 4-digit 7-segment display\n#define CLK 8 // Clock pin connected to D8\n#define DIO 9 // Data pin connected to D9\n#define BUTTON_PIN 6 // Push button connected to D6\nTM1637Display display(CLK, DIO); // Create display object\nint count = 0;     // Variable to store button press count\nint lastButtonState = LOW;\nvoid setup() {\n pinMode(BUTTON_PIN, INPUT);  // Set button pin as input\n display.setBrightness(7);   // Set display brightness (0 to 7)\n display.showNumberDec(0);   // Initialize display with 0\n}\nvoid loop() {\n int buttonState = digitalRead(BUTTON_PIN);\n // Detect button press (LOW to HIGH transition)\n if (buttonState == HIGH && lastButtonState == LOW) {\n  count++;           // Increment count\n  if (count > 9999) count = 0; // Reset after 9999\n  display.showNumberDec(count); // Display the count\n  delay(200);         // Debounce delay\n }\n lastButtonState = buttonState; // Store last button state\n}", "y2c8s7-explore-try-code": "#include <TM1637Display.h>\n#define CLK 8\n#define DIO 9\n#define BUTTON_PIN 5\nTM1637Display display(CLK, DIO);\nint count = 0;\nint lastButtonState = LOW;\nvoid setup() {\n  pinMode(BUTTON_PIN, INPUT);\n  display.setBrightness(7);\n  display.showNumberDec(0);\n}\nvoid loop() {\n  int buttonState = digitalRead(BUTTON_PIN);\n  // Detect button press\n  if (buttonState == HIGH && lastButtonState == LOW) {\n    count += 2;                   // Increase by 2 (even numbers)\n    if (count > 9998) count = 2;  // Reset safely within 4 digits\n    display.showNumberDec(count);\n    delay(200);                   // Debounce\n  }\n  lastButtonState = buttonState;\n}"});
  mergeCodes("2-8-7", "elaborate", {"y2c8s7-elaborate-project1-code": "// Session 7 Grade 8 Project 1\n// Elevator Floor System\n// Touch selects target floor, push confirms travel\n// Green LED = going up, Red LED = going down\n#include <TM1637Display.h>\nconst int touchSensor = 2;\nconst int pushButton = 7;\nconst int redLED = 10;\nconst int greenLED = 11;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int TOUCH_ACTIVE = HIGH; // change to LOW if opposite\nTM1637Display display(CLK, DIO);\nint currentFloor = 1;\nint targetFloor = 1;\nbool lastTouch = LOW;\nvoid setup() {\npinMode(touchSensor, INPUT);\npinMode(pushButton, INPUT_PULLUP);\npinMode(redLED, OUTPUT);\npinMode(greenLED, OUTPUT);\ndisplay.setBrightness(7);\ndisplay.showNumberDec(currentFloor, true);\n}\nvoid loop() {\nbool touchNow = digitalRead(touchSensor);\nif (touchNow == TOUCH_ACTIVE && lastTouch != TOUCH_ACTIVE) {\ntargetFloor++;\nif (targetFloor > 9) targetFloor = 1;\ndisplay.showNumberDec(targetFloor, true);\ndelay(300);\n}\nif (digitalRead(pushButton) == LOW) {\nmoveToTargetFloor();\n}\nlastTouch = touchNow;\n}\nvoid moveToTargetFloor() {\nif (targetFloor > currentFloor) {\ndigitalWrite(greenLED, HIGH);\ndigitalWrite(redLED, LOW);\nwhile (currentFloor < targetFloor) {\ncurrentFloor++;\ndisplay.showNumberDec(currentFloor, true);\ndelay(800);\n}\n} else if (targetFloor < currentFloor) {\ndigitalWrite(greenLED, LOW);\ndigitalWrite(redLED, HIGH);\nwhile (currentFloor > targetFloor) {\ncurrentFloor--;\ndisplay.showNumberDec(currentFloor, true);\ndelay(800);\n}\n}\ndigitalWrite(greenLED, LOW);\ndigitalWrite(redLED, LOW);\ndelay(300);\n}", "y2c8s7-elaborate-project2-code": "// Session 7 Grade 8 Project 2\n// Scoreboard\n// Touch selects Team A or Team B, Push increases selected team score\n// Display format: A score on left two digits, B score on right two digits\n#include <TM1637Display.h>\nconst int touchSensor = 2;\nconst int pushButton = 5;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int TOUCH_ACTIVE = HIGH; // change to LOW if opposite\nTM1637Display display(CLK, DIO);\nint scoreA = 0;\nint scoreB = 0;\nint selectedTeam = 1; // 1 = Team A, 2 = Team B\nbool lastTouch = LOW;\nbool lastButton = HIGH;\nvoid setup() {\npinMode(touchSensor, INPUT);\npinMode(pushButton, INPUT_PULLUP);\ndisplay.setBrightness(7);\nshowScore();\n}\nvoid loop() {\nbool touchNow = digitalRead(touchSensor);\nbool buttonNow = digitalRead(pushButton);\nif (touchNow == TOUCH_ACTIVE && lastTouch != TOUCH_ACTIVE) {\nselectedTeam = (selectedTeam == 1) ? 2 : 1;\nblinkSelectedTeam();\ndelay(300);\n}\nif (buttonNow == LOW && lastButton == HIGH) {\nif (selectedTeam == 1) {\nscoreA++;\nif (scoreA > 99) scoreA = 0;\n} else {\nscoreB++;\nif (scoreB > 99) scoreB = 0;\n}\nshowScore();\ndelay(300);\n}\nlastTouch = touchNow;\nlastButton = buttonNow;\n}\nvoid showScore() {\nint displayValue = scoreA * 100 + scoreB;\ndisplay.showNumberDec(displayValue, true);\n}\nvoid blinkSelectedTeam() {\ndisplay.showNumberDec(selectedTeam, true);\ndelay(500);\nshowScore();\n}"});
  mergeCodes("2-8-8", "explore", {"y2c8s8-explore-main-code": "// Touch Sensor with Two LEDs\n// D6 LED lights when sensor output is LOW\n// D7 LED lights when sensor output is HIGH\n#define TOUCH_PIN 2 // Touch sensor connected to D2\n#define LED_LOW 6  // LED for LOW state on D6\n#define LED_HIGH 7  // LED for HIGH state on D7\nvoid setup() {\n pinMode(TOUCH_PIN, INPUT); // Touch sensor input\n pinMode(LED_LOW, OUTPUT); // LED for LOW state\n pinMode(LED_HIGH, OUTPUT); // LED for HIGH state\n digitalWrite(LED_LOW, LOW);\n digitalWrite(LED_HIGH, LOW);\n}\nvoid loop() {\n int touchState = digitalRead(TOUCH_PIN);\n if (touchState == HIGH) {\n  // Touch detected → turn ON LED_HIGH, OFF LED_LOW\n  digitalWrite(LED_HIGH, HIGH);\n  digitalWrite(LED_LOW, LOW);\n } else {\n  // No touch → turn ON LED_LOW, OFF LED_HIGH\n  digitalWrite(LED_HIGH, LOW);\n  digitalWrite(LED_LOW, HIGH);\n }\n}", "y2c8s8-explore-try-code": "#define TOUCH_PIN 2   // Touch sensor connected to D2\n#define LED_LOW 6     // LED for LOW state\n#define LED_HIGH 7    // LED for HIGH state\n#define BUZZER_PIN 5  // Buzzer on D5\nvoid setup() {\n  pinMode(TOUCH_PIN, INPUT);\n  pinMode(LED_LOW, OUTPUT);\n  pinMode(LED_HIGH, OUTPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  digitalWrite(LED_LOW, LOW);\n  digitalWrite(LED_HIGH, LOW);\n  digitalWrite(BUZZER_PIN, LOW);\n}\nvoid loop() {\n  int touchState = digitalRead(TOUCH_PIN);\n  if (touchState == HIGH) {\n    // Touch detected\n    digitalWrite(LED_HIGH, HIGH);\n    digitalWrite(LED_LOW, LOW);\n    digitalWrite(BUZZER_PIN, HIGH); // Buzzer ON\n  } else {\n    // No touch\n    digitalWrite(LED_HIGH, LOW);\n    digitalWrite(LED_LOW, HIGH);\n    digitalWrite(BUZZER_PIN, LOW);  // Buzzer OFF\n  }\n}"});
  mergeCodes("2-8-8", "elaborate", {"y2c8s8-elaborate-project1-code": "// Touch Sensor with Two LEDs\n// D6 LED lights when sensor output is LOW\n// D7 LED lights when sensor output is HIGH\n#define TOUCH_PIN 2 // Touch sensor connected to D2\n#define LED_LOW 6  // LED for LOW state on D6\n#define LED_HIGH 7  // LED for HIGH state on D7\nvoid setup() {\n pinMode(TOUCH_PIN, INPUT); // Touch sensor input\n pinMode(LED_LOW, OUTPUT); // LED for LOW state\n pinMode(LED_HIGH, OUTPUT); // LED for HIGH state\n digitalWrite(LED_LOW, LOW);\n digitalWrite(LED_HIGH, LOW);\n}\nvoid loop() {\n int touchState = digitalRead(TOUCH_PIN);\n if (touchState == HIGH) {\n  // Touch detected → turn ON LED_HIGH, OFF LED_LOW\n  digitalWrite(LED_HIGH, HIGH);\n  digitalWrite(LED_LOW, LOW);\n } else {\n  // No touch → turn ON LED_LOW, OFF LED_HIGH\n  digitalWrite(LED_HIGH, LOW);\n  digitalWrite(LED_LOW, HIGH);\n }\n}"});
  mergeCodes("2-8-9", "explore", {"y2c8s9-explore-main-code": "#define LASER_PIN 2   // Laser diode on D2\n#define LDR_PIN 3     // LDR sensor on D3\n#define LED_PIN 5     // LED on D5\n#define BUZZER_PIN 6  // Buzzer on D6\nvoid setup() {\n  pinMode(LASER_PIN, OUTPUT);\n  pinMode(LDR_PIN, INPUT);\n  pinMode(LED_PIN, OUTPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  digitalWrite(LASER_PIN, HIGH); // Laser ON\n  digitalWrite(LED_PIN, LOW);    // LED OFF initially\n  digitalWrite(BUZZER_PIN, LOW); // Buzzer OFF initially\n}\nvoid loop() {\n  int ldrState = digitalRead(LDR_PIN);\n  if (ldrState == HIGH) {\n    // Beam normal\n    digitalWrite(LED_PIN, LOW);\n    digitalWrite(BUZZER_PIN, LOW);\n  } else {\n    // Beam broken → Alarm\n    digitalWrite(LED_PIN, HIGH);\n    digitalWrite(BUZZER_PIN, HIGH);\n  }\n}", "y2c8s9-explore-try-code": "#define LASER_PIN 2   // Laser diode on D2\n#define LDR_PIN 3     // LDR sensor on D3\n#define LED1_PIN 5    // LED1 on D5\n#define LED2_PIN 6    // LED2 on D6\nvoid setup() {\n  pinMode(LASER_PIN, OUTPUT);\n  pinMode(LDR_PIN, INPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  digitalWrite(LASER_PIN, HIGH); // Laser ON\n  digitalWrite(LED1_PIN, LOW);\n  digitalWrite(LED2_PIN, LOW);\n}\nvoid loop() {\n  int ldrState = digitalRead(LDR_PIN);\n  if (ldrState == HIGH) {\n    // Beam normal\n    digitalWrite(LED1_PIN, LOW);\n    digitalWrite(LED2_PIN, LOW);\n  } else {\n    // Beam broken\n    digitalWrite(LED1_PIN, HIGH);\n    digitalWrite(LED2_PIN, HIGH);\n  }\n}"});
  mergeCodes("2-8-9", "elaborate", {});
  mergeCodes("2-9-1", "explore", {"y2c9s1-explore-main-code": "#define LDR_PIN 2      // LDR sensor on D2\n#define LED1_PIN 5     // LED1 on D5\n#define LED2_PIN 6     // LED2 on D6\n#define LED3_PIN 7     // LED3 on D7\n#define BUZZER_PIN 3   // Buzzer on D3\nvoid setup() {\n  pinMode(LDR_PIN, INPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  pinMode(LED3_PIN, OUTPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  digitalWrite(LED1_PIN, LOW);\n  digitalWrite(LED2_PIN, LOW);\n  digitalWrite(LED3_PIN, LOW);\n  digitalWrite(BUZZER_PIN, LOW);\n}\nvoid loop() {\n  int ldrState = digitalRead(LDR_PIN);\n  // Most LDR modules: LOW = dark, HIGH = bright\n  if (ldrState == LOW) {\n    // Dark → turn everything ON\n    digitalWrite(LED1_PIN, HIGH);\n    digitalWrite(LED2_PIN, HIGH);\n    digitalWrite(LED3_PIN, HIGH);\n    digitalWrite(BUZZER_PIN, HIGH);\n  } else {\n    // Bright → turn everything OFF\n    digitalWrite(LED1_PIN, LOW);\n    digitalWrite(LED2_PIN, LOW);\n    digitalWrite(LED3_PIN, LOW);\n    digitalWrite(BUZZER_PIN, LOW);\n  }\n}", "y2c9s1-explore-try-code": "#define LDR_PIN 2      // LDR sensor on D2\n#define LED1_PIN 3     // LED1 on D3\n#define LED2_PIN 4     // LED2 on D4\n#define LED3_PIN 5     // LED3 on D5\n#define BUZZER_PIN 6   // Buzzer on D6\nvoid setup() {\n  pinMode(LDR_PIN, INPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  pinMode(LED3_PIN, OUTPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  digitalWrite(LED1_PIN, LOW);\n  digitalWrite(LED2_PIN, LOW);\n  digitalWrite(LED3_PIN, LOW);\n  digitalWrite(BUZZER_PIN, LOW);\n}\nvoid loop() {\n  int ldrState = digitalRead(LDR_PIN);\n  // Most LDR modules: LOW = dark, HIGH = bright\n  if (ldrState == LOW) {\n    // Dark → turn everything ON\n    digitalWrite(LED1_PIN, HIGH);\n    digitalWrite(LED2_PIN, HIGH);\n    digitalWrite(LED3_PIN, HIGH);\n    digitalWrite(BUZZER_PIN, HIGH);\n  } else {\n    // Bright → turn everything OFF\n    digitalWrite(LED1_PIN, LOW);\n    digitalWrite(LED2_PIN, LOW);\n    digitalWrite(LED3_PIN, LOW);\n    digitalWrite(BUZZER_PIN, LOW);\n  }\n}"});
  mergeCodes("2-9-1", "elaborate", {"y2c9s1-elaborate-project1-code": "// Session 1 Grade 9\n// Alarm - LDR + Buzzer + Push Button\nconst int ldrPin = A0;\nconst int pushButton = 5;\nconst int buzzer = 7;\nint darkValue = 500; // adjust after testing\nbool alarmStopped = false;\nvoid setup() {\npinMode(pushButton, INPUT_PULLUP);\npinMode(buzzer, OUTPUT);\n}\nvoid loop() {\nint ldrValue = analogRead(ldrPin);\nif (digitalRead(pushButton) == LOW) {\nalarmStopped = true;\ndigitalWrite(buzzer, LOW);\ndelay(300);\n}\nif (ldrValue > darkValue && alarmStopped == false) {\ndigitalWrite(buzzer, HIGH);\n} else {\ndigitalWrite(buzzer, LOW);\n}\n// When light comes back, alarm can trigger again next time it becomes dark.\nif (ldrValue < darkValue) {\nalarmStopped = false;\n}\n}", "y2c9s1-elaborate-project2-code": "#define LDR_PIN 2      // LDR sensor on D2\n#define LED1_PIN 3     // LED1 on D3\n#define LED2_PIN 4     // LED2 on D4\n#define LED3_PIN 5     // LED3 on D5\n#define BUZZER_PIN 6   // Buzzer on D6\nvoid setup() {\n  pinMode(LDR_PIN, INPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  pinMode(LED3_PIN, OUTPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  digitalWrite(LED1_PIN, LOW);\n  digitalWrite(LED2_PIN, LOW);\n  digitalWrite(LED3_PIN, LOW);\n  digitalWrite(BUZZER_PIN, LOW);\n}\nvoid loop() {\n  int ldrState = digitalRead(LDR_PIN);\n  // Most LDR modules: LOW = dark, HIGH = bright\n  if (ldrState == LOW) {\n    // Dark → turn everything ON\n    digitalWrite(LED1_PIN, HIGH);\n    digitalWrite(LED2_PIN, HIGH);\n    digitalWrite(LED3_PIN, HIGH);\n    digitalWrite(BUZZER_PIN, HIGH);\n  } else {\n    // Bright → turn everything OFF\n    digitalWrite(LED1_PIN, LOW);\n    digitalWrite(LED2_PIN, LOW);\n    digitalWrite(LED3_PIN, LOW);\n    digitalWrite(BUZZER_PIN, LOW);\n  }\n}"});
  mergeCodes("2-9-10", "explore", {"y2c9s10-explore-main-code": "#include <TM1637Display.h> // Include library for 4-digit 7-segment display\n#define CLK 8 // Clock pin connected to D8\n#define DIO 9 // Data pin connected to D9\n#define BUTTON_PIN 2 // Push button connected to D2\nTM1637Display display(CLK, DIO); // Create display object\nint count = 0;     // Variable to store button press count\nint lastButtonState = LOW;\nvoid setup() {\n pinMode(BUTTON_PIN, INPUT);  // Set button pin as input\n display.setBrightness(7);   // Set display brightness (0 to 7)\n display.showNumberDec(0);   // Initialize display with 0\n}\nvoid loop() {\n int buttonState = digitalRead(BUTTON_PIN);\n // Detect button press (LOW to HIGH transition)\n if (buttonState == HIGH && lastButtonState == LOW) {\n  count++;           // Increment count\n  if (count > 9999) count = 0; // Reset after 9999\n  display.showNumberDec(count); // Display the count\n  delay(200);         // Debounce delay\n }\n lastButtonState = buttonState; // Store last button state\n}", "y2c9s10-explore-try-code": "#include <TM1637Display.h>\n#define CLK 8\n#define DIO 9\n#define BUTTON_PIN 5\nTM1637Display display(CLK, DIO);\nint count = 0;\nbool running = false;\nint lastButtonState = LOW;\nunsigned long lastPressTime = 0;\nint pressCount = 0;\nunsigned long lastCountTime = 0;\nvoid setup() {\n  pinMode(BUTTON_PIN, INPUT);\n  display.setBrightness(7);\n  display.showNumberDec(0);\n}\nvoid loop() {\n  int buttonState = digitalRead(BUTTON_PIN);\n  // Detect button press\n  if (buttonState == HIGH && lastButtonState == LOW) {\n    pressCount++;\n    lastPressTime = millis();\n    delay(50);\n  }\n\n // Double press detection (within 400 ms)\n if (pressCount == 2 && (millis() - lastPressTime) < 400) {\n running = true;\n count = 1; // Start from 1\n display.showNumberDec(count);\n lastCountTime = millis();\n pressCount = 0;\n delay(200);\n }\n // Reset press count if too slow\n if ((millis() - lastPressTime) > 400) {\n pressCount = 0;\n }\n // Auto counting\n if (running) {\n if (millis() - lastCountTime >= 500) {\n count++;\n if (count > 9999) count = 1;\n display.showNumberDec(count);\n lastCountTime = millis();\n }\n // Stop and reset after 3 seconds\n if (millis() - lastPressTime > 3000) {\n running = false;\n count = 0;\n display.showNumberDec(0);\n }\n }\n lastButtonState = buttonState;\n}"});
  mergeCodes("2-9-10", "elaborate", {"y2c9s10-elaborate-project1-code": "// Session 10 Grade 9\n// Smart Bathroom Timer + Mirror Light\n// Hall sensor detects door closed and shows closed minutes on 7\nsegment\n// IR sensor detects person near mirror and turns string LED ON\n// Hall sensor is used as DIGITAL sensor with digitalRead(A0)\n#include <TM1637Display.h>\nconst int hallSensor = A0;\nconst int irSensor = 2;\nconst int stringLED = 5;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int MAGNET_DETECTED = LOW; // change to HIGH if opposite\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nTM1637Display display(CLK, DIO);\nbool doorClosed = false;\nunsigned long doorCloseStart = 0;\nvoid setup() {\npinMode(hallSensor, INPUT);\npinMode(irSensor, INPUT);\npinMode(stringLED, OUTPUT);\ndisplay.setBrightness(7);\ndisplay.showNumberDec(0, true);\n}"});
  mergeCodes("2-9-2", "explore", {"y2c9s2-explore-main-code": "// Sound Level → TM1637 dB Display\n// Sensor: analog output on A3\n// Display: TM1637 4-digit (CLK=D8, DIO=D9)\n// Board: Arduino UNO (or compatible)\n#include <Arduino.h>\n#include <TM1637Display.h>\n#include <math.h>\n#define SOUND_PIN A3 // Analog output from sound sensor\n#define CLK_PIN 8  // TM1637 CLK\n#define DIO_PIN 9  // TM1637 DIO\nTM1637Display display(CLK_PIN, DIO_PIN);\n// ---- Tuning knobs ----\nconst unsigned long SAMPLE_MS = 50; // sampling window\nconst float VCC_VOLTS = 5.0;     // your board supply\nconst float REF_VOLTS = 0.010f;   // ~10 mV reference for dB calc\nconst int CAL_OFFSET= 30;     // shift reading up/down after log calc\nvoid setup() {\n Serial.begin(9600);\n display.setBrightness(7);     // 0..7\n display.clear();\n}\nint readDb() {\n unsigned long start = millis();\n int signalMax = 0;\n int signalMin = 1023;\n // Capture peak-to-peak over a short window\n while (millis() - start < SAMPLE_MS) {\n  int sample = analogRead(SOUND_PIN);\n  if (sample > signalMax) signalMax = sample;\n  if (sample < signalMin) signalMin = sample;\n }\n int p2p = signalMax - signalMin;       // peak-to-peak counts\n float volts = p2p * (VCC_VOLTS / 1023.0f);  // convert to volts\n\n if (volts < 0.001f) volts = 0.001f;\n float dB = 20.0f * log10f(volts / REF_VOLTS);\n dB += CAL_OFFSET;\n if (dB < 0) dB = 0;\n if (dB > 99) dB = 99;\n return (int)(dB + 0.5f);\n}\nvoid loop() {\n int dB = readDb();\n Serial.print(\"Approx dB: \");\n Serial.println(dB);\n display.showNumberDec(dB, true);\n delay(50);\n}", "y2c9s2-explore-try-code": "#include <Arduino.h>\n#include <TM1637Display.h>\n#include <math.h>\n#define SOUND_PIN A3\n#define CLK_PIN   8\n#define DIO_PIN   9\n#define LED_PIN    6   // LED on D6\n#define BUZZER_PIN 5   // Buzzer on D5\nTM1637Display display(CLK_PIN, DIO_PIN);\n// ---- Tuning knobs ----\nconst unsigned long SAMPLE_MS = 50;\nconst float VCC_VOLTS = 5.0;\nconst float REF_VOLTS = 0.010f;\nconst int   CAL_OFFSET= 30;\n// Threshold for alert\nconst int SOUND_THRESHOLD = 60; // dB level for LED & buzzer\nvoid setup() {\n  Serial.begin(9600);\n  pinMode(LED_PIN, OUTPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  display.setBrightness(7);\n  display.clear();\n}\nint readDb() {\n  unsigned long start = millis();\n  int signalMax = 0;\n  int signalMin = 1023;\n\n while (millis() - start < SAMPLE_MS) {\n int sample = analogRead(SOUND_PIN);\n if (sample > signalMax) signalMax = sample;\n if (sample < signalMin) signalMin = sample;\n }\n int p2p = signalMax - signalMin;\n float volts = p2p * (VCC_VOLTS / 1023.0f);\n if (volts < 0.001f) volts = 0.001f;\n float dB = 20.0f * log10f(volts / REF_VOLTS);\n dB += CAL_OFFSET;\n if (dB < 0) dB = 0;\n if (dB > 99) dB = 99;\n return (int)(dB + 0.5f);\n}\nvoid loop() {\n int dB = readDb();\n Serial.print(\"Approx dB: \");\n Serial.println(dB);\n display.showNumberDec(dB, true);\n // LED + Buzzer control\n if (dB >= SOUND_THRESHOLD) {\n digitalWrite(LED_PIN, HIGH); // LED ON\n digitalWrite(BUZZER_PIN, HIGH); // Buzzer ON\n } else {\n digitalWrite(LED_PIN, LOW); // LED OFF\n digitalWrite(BUZZER_PIN, LOW); // Buzzer OFF\n }\n delay(50);\n}"});
  mergeCodes("2-9-2", "elaborate", {"y2c9s2-elaborate-project1-code": "// Session 2 Grade 9\n// Shout Meter\n// More sound = Servo moves more\n#include <Servo.h>\nconst int soundSensor = A0;\nconst int servoPin = 4;\nServo meterServo;\nvoid setup() {\nmeterServo.attach(servoPin);\nmeterServo.write(0);\n}\nvoid loop() {\nint soundValue = analogRead(soundSensor);\nint angle = map(soundValue, 300, 900, 0, 180);\nangle = constrain(angle, 0, 180);\nmeterServo.write(angle);\ndelay(100);\n}", "y2c9s2-elaborate-project2-code": "// Session 2 Grade 9\n// Sound Level Meter\n// For RGB LED project, D12 is always HIGH\nconst int soundSensor = A0;\nconst int redLED = 10;\nconst int yellowLED = 11;\nconst int alwaysHighPin = 12;\nvoid setup() {\npinMode(redLED, OUTPUT);\npinMode(yellowLED, OUTPUT);\npinMode(alwaysHighPin, OUTPUT);\ndigitalWrite(alwaysHighPin, HIGH); // D12 always HIGH\n}\nvoid loop() {\nint soundValue = analogRead(soundSensor);\ndigitalWrite(alwaysHighPin, HIGH); // keep D12 always HIGH\nif (soundValue < 450) {\ndigitalWrite(redLED, LOW);\ndigitalWrite(yellowLED, LOW);\n} else if (soundValue < 700) {\ndigitalWrite(redLED, LOW);\ndigitalWrite(yellowLED, HIGH);\n} else {\ndigitalWrite(redLED, HIGH);\ndigitalWrite(yellowLED, HIGH);\n}\ndelay(100);\n}"});
  mergeCodes("2-9-3", "explore", {"y2c9s3-explore-main-code": "/// Tilt → Servo demo\n// Moves a Servo on D3 to 180° when the tilt sensor is HIGH, and back to 0° otherwise.\n#include <Servo.h>\n#define TILT_PIN 2   // Tilt sensor connected to D2 (digital input)\n#define SERVO_PIN 3  // Servo signal connected to D3\nServo tiltServo;\nvoid setup() {\n pinMode(TILT_PIN, INPUT); // Use INPUT_PULLUP if your tilt switch is wired to GND\n tiltServo.attach(SERVO_PIN);\n tiltServo.write(0);    // Start at 0°\n}\nvoid loop() {\n int tiltState = digitalRead(TILT_PIN);\n if (tiltState == HIGH) {\n  tiltServo.write(180);  // Tilt detected → move to 180°\n } else {\n  tiltServo.write(0);   // No tilt → move back to 0°\n }\n delay(20); // small delay to reduce jitter\n}", "y2c9s3-explore-try-code": "#include <Servo.h>\n#define TILT_PIN 2 // Tilt sensor on D2\n#define SERVO_PIN 3 // Servo on D3\n#define LED_PIN 6 // LED on D6\n#define BUZZER_PIN 5 // Buzzer on D5\nServo tiltServo;\nvoid setup() {\n pinMode(TILT_PIN, INPUT); // Use INPUT_PULLUP if needed\n pinMode(LED_PIN, OUTPUT);\n pinMode(BUZZER_PIN, OUTPUT);\n tiltServo.attach(SERVO_PIN);\n tiltServo.write(0); // Start at 0°\n digitalWrite(LED_PIN, LOW);\n digitalWrite(BUZZER_PIN, LOW);\n}\nvoid loop() {\n int tiltState = digitalRead(TILT_PIN);\n if (tiltState == HIGH) {\n tiltServo.write(180); // Move Servo\n digitalWrite(LED_PIN, HIGH); // LED ON\n digitalWrite(BUZZER_PIN, HIGH); // Buzzer ON\n } else {\n tiltServo.write(0); // Reset Servo\n digitalWrite(LED_PIN, LOW); // LED OFF\n digitalWrite(BUZZER_PIN, LOW); // Buzzer OFF\n }\n delay(20);\n}"});
  mergeCodes("2-9-3", "elaborate", {"y2c9s3-elaborate-project1-code": "// Session 3 Grade 9\n// Auto Reverse Gear\n// Push button = motor forward\n// Tilt + push button = motor reverse\nconst int tiltSensor = 2;\nconst int motorPin1 = 5;\nconst int motorPin2 = 6;\nconst int pushButton = 7;\nconst int TILT_ACTIVE = HIGH;  // change to LOW if opposite\nvoid setup() {\npinMode(tiltSensor, INPUT);\npinMode(pushButton, INPUT_PULLUP);\npinMode(motorPin1, OUTPUT);\npinMode(motorPin2, OUTPUT);\nstopMotor();\n}", "y2c9s3-elaborate-project2-code": "const int tiltPin = 2;\nconst int redLED = 7;\nconst int greenLED = 6;\nvoid setup() {\npinMode(tiltPin, INPUT_PULLUP);\npinMode(redLED, OUTPUT);\npinMode(greenLED, OUTPUT);\n}\nvoid loop() {\nif (digitalRead(tiltPin) == LOW) {\n// Sensor in one position (e.g., Horizontal)\ndigitalWrite(redLED, HIGH);\ndigitalWrite(greenLED, LOW);\n} else {\n// Sensor in the other position (e.g., Tilted/Vertical)\ndigitalWrite(redLED, LOW);\ndigitalWrite(greenLED, HIGH);\n}\n}"});
  mergeCodes("2-9-4", "explore", {"y2c9s4-explore-main-code": "#define HALL_PIN 2   // Hall sensor output pin\n#define BUZZ1_PIN 6  // Buzzer 1\n#define BUZZ2_PIN 5  // Buzzer 2\nvoid setup() {\n  pinMode(HALL_PIN, INPUT);\n  pinMode(BUZZ1_PIN, OUTPUT);\n  pinMode(BUZZ2_PIN, OUTPUT);\n  digitalWrite(BUZZ1_PIN, LOW);\n  digitalWrite(BUZZ2_PIN, LOW);\n}\nvoid loop() {\n  int hallState = digitalRead(HALL_PIN);\n  if (hallState == LOW) {  // Magnet detected\n    digitalWrite(BUZZ1_PIN, HIGH); // Buzzer1 ON\n    digitalWrite(BUZZ2_PIN, HIGH); // Buzzer2 ON\n  } else {\n    digitalWrite(BUZZ1_PIN, LOW);  // Buzzer1 OFF\n    digitalWrite(BUZZ2_PIN, LOW);  // Buzzer2 OFF\n  }\n}", "y2c9s4-explore-try-code": "#define HALL_PIN 2    // Hall sensor output pin\n#define BUZZ1_PIN 6   // Buzzer 1\n#define BUZZ2_PIN 7   // Buzzer 2\n#define LED1_PIN 5    // LED 1\n#define LED2_PIN 4    // LED 2\nvoid setup() {\n  pinMode(HALL_PIN, INPUT);\n  pinMode(BUZZ1_PIN, OUTPUT);\n  pinMode(BUZZ2_PIN, OUTPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  // Default state (no magnet)\n  digitalWrite(BUZZ1_PIN, LOW);\n  digitalWrite(BUZZ2_PIN, LOW);\n  digitalWrite(LED1_PIN, HIGH);\n  digitalWrite(LED2_PIN, HIGH);\n}\nvoid loop() {\n  int hallState = digitalRead(HALL_PIN);\n  if (hallState == LOW) {  // Magnet detected\n    // Buzzers ON\n    digitalWrite(BUZZ1_PIN, HIGH);\n    digitalWrite(BUZZ2_PIN, HIGH);\n\n\n// LEDs OFF (opposite behavior)\n digitalWrite(LED1_PIN, LOW);\n digitalWrite(LED2_PIN, LOW);\n } else {\n // Buzzers OFF\n digitalWrite(BUZZ1_PIN, LOW);\n digitalWrite(BUZZ2_PIN, LOW);\n // LEDs ON\n digitalWrite(LED1_PIN, HIGH);\n digitalWrite(LED2_PIN, HIGH);\n }\n}"});
  mergeCodes("2-9-4", "elaborate", {"y2c9s4-elaborate-project1-code": "// Session 4 Grade 9\n// Duck\n// Leaf magnet near duck mouth sensor = duck legs\nmotor ON\n// Magnetic sensor used as DIGITAL sensor on A0\nconst int magneticSensor = A0;\nconst int motor = 5;\nconst int MAGNET_DETECTED = LOW; // change to\nHIGH if opposite\nvoid setup() {\npinMode(magneticSensor, INPUT);\npinMode(motor, OUTPUT);\n}\nvoid loop() {\nif (digitalRead(magneticSensor) ==\nMAGNET_DETECTED) {\ndigitalWrite(motor, HIGH);\n} else {\ndigitalWrite(motor, LOW);\n}\n}", "y2c9s4-elaborate-project2-code": "// Session 4 Grade 9\n// Magnetic Locker\n// Magnet detected = door opens\n// Magnetic sensor used as DIGITAL sensor on A0\n#include <Servo.h>\nconst int magneticSensor = A0;\nconst int servoPin = 4;\nconst int MAGNET_DETECTED = LOW; // change to HIGH if opposite\nServo lockerServo;\nvoid setup() {\npinMode(magneticSensor, INPUT);\nlockerServo.attach(servoPin);\nlockerServo.write(0); // locker closed\n}\nvoid loop() {\nif (digitalRead(magneticSensor) == MAGNET_DETECTED) {\nlockerServo.write(90); // locker open\n} else {\nlockerServo.write(0); // locker closed\n}\n}"});
  mergeCodes("2-9-5", "explore", {"y2c9s5-explore-main-code": "#define IR_PIN 2     // IR sensor on D2\n#define LED1 3       // LED1\n#define LED2 4       // LED2\n#define LED3 5       // LED3\n#define LED4 6       // LED4\nint count = 0;\nint lastIRState = HIGH;\nvoid setup() {\n  pinMode(IR_PIN, INPUT);\n  pinMode(LED1, OUTPUT);\n  pinMode(LED2, OUTPUT);\n  pinMode(LED3, OUTPUT);\n  pinMode(LED4, OUTPUT);\n  // All OFF initially\n  digitalWrite(LED1, LOW);\n  digitalWrite(LED2, LOW);\n  digitalWrite(LED3, LOW);\n  digitalWrite(LED4, LOW);\n}\nvoid loop() {\n  int irState = digitalRead(IR_PIN);\n  // Detect object (LOW = detected)\n  if (irState == LOW && lastIRState == HIGH) {\n    count++;\n    // Control LEDs based on count\n    if (count == 1) {\n      digitalWrite(LED1, HIGH);\n    }\n    else if (count == 2) {\n\n digitalWrite(LED2, HIGH);\n }\n else if (count == 3) {\n digitalWrite(LED3, HIGH);\n }\n else if (count == 4) {\n digitalWrite(LED4, HIGH);\n }\n else {\n // Reset everything\n digitalWrite(LED1, LOW);\n digitalWrite(LED2, LOW);\n digitalWrite(LED3, LOW);\n digitalWrite(LED4, LOW);\n count = 0;\n }\n delay(200); // debounce\n }\n lastIRState = irState;\n}", "y2c9s5-explore-try-code": "#define IR_PIN 2        // IR sensor connected to digital pin 2\n#define BUZZER_PIN 5    // Buzzer connected to digital pin 5\nint count = 0;          // Variable to store number of detections\nint lastIRState = HIGH; // Store previous IR state to detect new object\nvoid setup() {\n  pinMode(IR_PIN, INPUT);        // Set IR sensor pin as input\n  pinMode(BUZZER_PIN, OUTPUT);   // Set buzzer pin as output\n  digitalWrite(BUZZER_PIN, LOW); // Keep buzzer OFF initially\n}\n// Function to beep 'n' times\nvoid beep(int times) {\n  for (int i = 0; i < times; i++) {     // Loop 'times' number of beeps\n    digitalWrite(BUZZER_PIN, HIGH);     // Turn buzzer ON\n    delay(200);                         // Wait 200 ms\n    digitalWrite(BUZZER_PIN, LOW);      // Turn buzzer OFF\n    delay(200);                         // Gap between beeps\n  }\n}\nvoid loop() {\n  int irState = digitalRead(IR_PIN); // Read IR sensor value\n  // Check if object is detected (LOW) and previous state was HIGH\n  if (irState == LOW && lastIRState == HIGH) {\n    count++;                         // Increase detection count\n    if (count >= 1 && count <= 4) {\n      beep(count);                   // Beep equal to count number\n    } else {\n      count = 0;                     // Reset after 4 detections\n    }\n\n delay(200); // Debounce delay to avoid multiple counts\n }\n lastIRState = irState; // Store current state for next loop\n}"});
  mergeCodes("2-9-5", "elaborate", {"y2c9s5-elaborate-project1-code": "// Session 5 Grade 9 Project 1\n// Smart Tap\n// Hand near tap = water droplet comes out\n#include <Servo.h>\nconst int irSensor = 2;\nconst int servoPin = 3;\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nServo tapServo;\nvoid setup() {\npinMode(irSensor, INPUT);\ntapServo.attach(servoPin);\ntapServo.write(0); // tap off\n}\nvoid loop() {\nif (digitalRead(irSensor) == IR_DETECTED) {\ntapServo.write(90); // water droplet out\n} else {\ntapServo.write(0); // water droplet back\n}\n}", "y2c9s5-elaborate-project2-code": "// Session 5 Grade 9 Project 2\n// IR Servo Gate\n// IR detected = gate opens for 5 seconds\n#include <Servo.h>\nconst int irSensor = 2;\nconst int servoPin = 3;\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nServo gateServo;\nvoid setup() {\npinMode(irSensor, INPUT);\ngateServo.attach(servoPin);\ngateServo.write(0); // gate closed\n}\nvoid loop() {\nif (digitalRead(irSensor) == IR_DETECTED) {\ngateServo.write(90); // gate open\ndelay(5000);\ngateServo.write(0); // gate closed\ndelay(500);\n}\n}"});
  mergeCodes("2-9-6", "explore", {"y2c9s6-explore-main-code": "#include <Servo.h>\n#define IR_PIN 2      // IR sensor on D2\n#define SERVO_PIN 3   // Servo on D3\nServo myServo;\nvoid setup() {\n  pinMode(IR_PIN, INPUT);\n  myServo.attach(SERVO_PIN);\n  myServo.write(0); // Start at 0°\n}\nvoid loop() {\n  int irState = digitalRead(IR_PIN);\n  if (irState == LOW) {  // Object detected\n    // Move from 0 → 180\n    for (int angle = 0; angle <= 180; angle++) {\n      myServo.write(angle);\n      delay(10);\n    }\n  } else {\n    // Return to 0\n    myServo.write(0);\n  }\n}", "y2c9s6-explore-try-code": "#include <Servo.h>\n#define IR_PIN 2      // IR sensor on D2\n#define SERVO_PIN 3   // Servo on D3\n#define LED_PIN 5     // LED on D5\nServo myServo;\nvoid setup() {\n  pinMode(IR_PIN, INPUT);\n  pinMode(LED_PIN, OUTPUT);\n  myServo.attach(SERVO_PIN);\n  myServo.write(0); // Start at 0°\n  digitalWrite(LED_PIN, LOW);\n}\nvoid loop() {\n  int irState = digitalRead(IR_PIN);\n  if (irState == LOW) {  // Object detected\n    digitalWrite(LED_PIN, HIGH); // LED ON\n    // Move Servo 0 → 180\n    for (int angle = 0; angle <= 180; angle++) {\n      myServo.write(angle);\n      delay(10);\n    }\n  } else {\n    digitalWrite(LED_PIN, LOW); // LED OFF\n    myServo.write(0);           // Return to 0°\n  }\n}"});
  mergeCodes("2-9-6", "elaborate", {"y2c9s6-elaborate-project1-code": "// Session 6 Grade 9 Project 1\n// Touch Servo Gate\n// Touch sensor touched = gate opens for 5 seconds\n#include <Servo.h>\nconst int touchSensor = 2;\nconst int servoPin = 3;\nconst int TOUCH_ACTIVE = HIGH; // change to LOW if opposite\nServo gateServo;\nvoid setup() {\npinMode(touchSensor, INPUT);\ngateServo.attach(servoPin);\ngateServo.write(0); // gate closed\n}\nvoid loop() {\nif (digitalRead(touchSensor) == TOUCH_ACTIVE) {\ngateServo.write(90); // gate open\ndelay(5000);\ngateServo.write(0); // gate close\ndelay(500);\n}\n}", "y2c9s6-elaborate-project2-code": "#include <Servo.h>\n#define IR_PIN 2      // IR sensor on D2\n#define SERVO_PIN 3   // Servo on D3\n#define LED_PIN 5     // LED on D5\nServo myServo;\nvoid setup() {\n  pinMode(IR_PIN, INPUT);\n  pinMode(LED_PIN, OUTPUT);\n  myServo.attach(SERVO_PIN);\n  myServo.write(0); // Start at 0°\n  digitalWrite(LED_PIN, LOW);\n}\nvoid loop() {\n  int irState = digitalRead(IR_PIN);\n  if (irState == LOW) {  // Object detected\n    digitalWrite(LED_PIN, HIGH); // LED ON\n    // Move Servo 0 → 180\n    for (int angle = 0; angle <= 180; angle++) {\n      myServo.write(angle);\n      delay(10);\n    }\n  } else {\n    digitalWrite(LED_PIN, LOW); // LED OFF\n    myServo.write(0);           // Return to 0°\n  }\n}"});
  mergeCodes("2-9-7", "explore", {"y2c9s7-explore-main-code": "// Sound Level → TM1637 dB Display\n// Sensor: analog output on A3\n// Display: TM1637 4-digit (CLK=D8, DIO=D9)\n// Board: Arduino UNO (or compatible)\n#include <Arduino.h>\n#include <TM1637Display.h>\n#include <math.h>\n#define SOUND_PIN A3 // Analog output from sound sensor\n#define CLK_PIN 8  // TM1637 CLK\n#define DIO_PIN 9  // TM1637 DIO\nTM1637Display display(CLK_PIN, DIO_PIN);\n// ---- Tuning knobs ----\nconst unsigned long SAMPLE_MS = 50; // sampling window\nconst float VCC_VOLTS = 5.0;     // your board supply\nconst float REF_VOLTS = 0.010f;   // ~10 mV reference for dB calc\nconst int CAL_OFFSET= 30;     // shift reading up/down after log calc\nvoid setup() {\n Serial.begin(9600);\n display.setBrightness(7);     // 0..7\n display.clear();\n}\nint readDb() {\n unsigned long start = millis();\n int signalMax = 0;\n int signalMin = 1023;\n // Capture peak-to-peak over a short window\n while (millis() - start < SAMPLE_MS) {\n  int sample = analogRead(SOUND_PIN);\n  if (sample > signalMax) signalMax = sample;\n  if (sample < signalMin) signalMin = sample;\n }\n int p2p = signalMax - signalMin;       // peak-to-peak counts\n float volts = p2p * (VCC_VOLTS / 1023.0f);  // convert to volts\n\n if (volts < 0.001f) volts = 0.001f;\n float dB = 20.0f * log10f(volts / REF_VOLTS);\n dB += CAL_OFFSET;\n if (dB < 0) dB = 0;\n if (dB > 99) dB = 99;\n return (int)(dB + 0.5f);\n}\nvoid loop() {\n int dB = readDb();\n Serial.print(\"Approx dB: \");\n Serial.println(dB);\n display.showNumberDec(dB, true);\n delay(50);\n}", "y2c9s7-explore-try-code": "#include <Arduino.h>\n#include <TM1637Display.h>\n#define SOUND_PIN A3\n#define CLK_PIN   8\n#define DIO_PIN   9\nTM1637Display display(CLK_PIN, DIO_PIN);\n// Clap settings\nconst int CLAP_THRESHOLD = 600;   // Adjust based on your sensor\nconst int RESET_TIME = 3000;      // 3 seconds\nint count = 0;\nunsigned long lastClapTime = 0;\nbool clapDetected = false;\nvoid setup() {\n  Serial.begin(9600);\n  display.setBrightness(7);\n  display.showNumberDec(0);\n}\nvoid loop() {\n  int soundValue = analogRead(SOUND_PIN);\n  Serial.println(soundValue);\n  // Detect clap (sound spike)\n  if (soundValue > CLAP_THRESHOLD && !clapDetected) {\n    count++;                       // Increase count\n    if (count > 9999) count = 0;\n\n display.showNumberDec(count);\n lastClapTime = millis(); // Store clap time\n clapDetected = true; // Avoid multiple triggers\n }\n // Reset clap detection when sound goes low\n if (soundValue < CLAP_THRESHOLD - 50) {\n clapDetected = false;\n }\n // Reset after 3 seconds of no clap\n if (millis() - lastClapTime > RESET_TIME) {\n count = 0;\n display.showNumberDec(count);\n }\n delay(50);\n}"});
  mergeCodes("2-9-7", "elaborate", {"y2c9s7-elaborate-project1-code": "// Session 7 Grade 9 Project 1\n// Entry Exit Occupancy Counter\n// IR1 = person entered, IR2 = person left\n// Display shows number of persons inside\n#include <TM1637Display.h>\nconst int entryIR = 2;\nconst int exitIR = 3;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int IR_DETECTED = LOW; // change to HIGH if opposite\nTM1637Display display(CLK, DIO);\nint insideCount = 0;\nbool lastEntry = HIGH;\nbool lastExit = HIGH;\nvoid setup() {\npinMode(entryIR, INPUT);\npinMode(exitIR, INPUT);\ndisplay.setBrightness(7);\ndisplay.showNumberDec(insideCount, true);\n}", "y2c9s7-elaborate-project2-code": "// Session 7 Grade 9 Project 2\n// Stopwatch\n// Push button cycles: START -> PAUSE -> RESET\n// Touch sensor can also reset anytime\n#include <TM1637Display.h>\nconst int touchSensor = 2;\nconst int pushButton = 5;\nconst int CLK = 9;\nconst int DIO = 8;\nconst int TOUCH_ACTIVE = HIGH; // change to LOW if opposite\nTM1637Display display(CLK, DIO);\nint mode = 0; // 0 reset, 1 running, 2 paused\nunsigned long startMillis = 0;\nunsigned long pausedTime = 0;\nunsigned long elapsedMillis = 0;\nbool lastButton = HIGH;\nvoid setup() {\npinMode(touchSensor, INPUT);\npinMode(pushButton, INPUT_PULLUP);\ndisplay.setBrightness(7);\nshowTime(0);\n}"});
  mergeCodes("2-9-8", "explore", {"y2c9s8-explore-main-code": "#define TOUCH_PIN 2   // Touch sensor on D2\n#define LED1 3        // LED1\n#define LED2 4        // LED2\n#define LED3 5        // LED3\n#define LED4 6        // LED4\nint count = 0;           // Store number of touches\nint lastTouchState = LOW; // Store previous touch state\nvoid setup() {\n  pinMode(TOUCH_PIN, INPUT);\n  pinMode(LED1, OUTPUT);\n  pinMode(LED2, OUTPUT);\n  pinMode(LED3, OUTPUT);\n  pinMode(LED4, OUTPUT);\n  // All LEDs OFF initially\n  digitalWrite(LED1, LOW);\n  digitalWrite(LED2, LOW);\n  digitalWrite(LED3, LOW);\n  digitalWrite(LED4, LOW);\n}\nvoid loop() {\n  int touchState = digitalRead(TOUCH_PIN);\n  // Detect new touch (LOW → HIGH)\n  if (touchState == HIGH && lastTouchState == LOW) {\n    count++;   // Increase touch count\n    if (count == 1) {\n      digitalWrite(LED1, HIGH); // Turn ON LED1\n    }\n    else if (count == 2) {\n      digitalWrite(LED2, HIGH); // Turn ON LED2\n    }\n\n\n else if (count == 3) {\n digitalWrite(LED3, HIGH); // Turn ON LED3\n }\n else if (count == 4) {\n digitalWrite(LED4, HIGH); // Turn ON LED4\n }\n else {\n // Reset all LEDs\n digitalWrite(LED1, LOW);\n digitalWrite(LED2, LOW);\n digitalWrite(LED3, LOW);\n digitalWrite(LED4, LOW);\n count = 0; // Reset count\n }\n delay(200); // Debounce\n }\n lastTouchState = touchState; // Store current state\n}", "y2c9s8-explore-try-code": "#define TOUCH_PIN 2     // Touch sensor on D2\n#define BUZZER_PIN 5    // Buzzer on D5\nint count = 0;             // Store touch count\nint lastTouchState = LOW;  // Store previous state\nvoid setup() {\n  pinMode(TOUCH_PIN, INPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  digitalWrite(BUZZER_PIN, LOW);\n}\n// Function to beep 'n' times\nvoid beep(int times) {\n  for (int i = 0; i < times; i++) {\n    digitalWrite(BUZZER_PIN, HIGH); // ON\n    delay(200);\n    digitalWrite(BUZZER_PIN, LOW);  // OFF\n    delay(200);\n  }\n}\nvoid loop() {\n  int touchState = digitalRead(TOUCH_PIN);\n  // Detect touch (LOW → HIGH)\n  if (touchState == HIGH && lastTouchState == LOW) {\n    count++;\n\n if (count >= 1 && count <= 4) {\n beep(count); // Beep based on count\n } else {\n count = 0; // Reset after 4\n }\n delay(200); // Debounce\n }\n lastTouchState = touchState;\n}"});
  mergeCodes("2-9-8", "elaborate", {"y2c9s8-elaborate-project1-code": "// Session 8 Grade 9\n// LDR Alarm Timer\n// LDR detects light/dark change and timer starts\n// Push button turns off / resets the timer\n#include <TM1637Display.h>\nconst int ldrSensor = A0;\nconst int pushButton = 5;\nconst int CLK = 9;\nconst int DIO = 8;\nint darkValue = 500; // adjust based on room light\nTM1637Display display(CLK, DIO);\nbool timerRunning = false;\nunsigned long startTime = 0;\nvoid setup() {\npinMode(pushButton, INPUT_PULLUP);\ndisplay.setBrightness(7);\ndisplay.showNumberDec(0, true);\n}"});
  mergeCodes("2-9-9", "explore", {"y2c9s9-explore-main-code": "#define LASER_PIN 2   // Laser diode on D2\n#define LDR_PIN 3     // LDR sensor on D3\n#define IR_PIN 4      // IR sensor on D4\n#define LED1_PIN 5    // LED1 on D5\n#define LED2_PIN 6    // LED2 on D6\nvoid setup() {\n  pinMode(LASER_PIN, OUTPUT);\n  pinMode(LDR_PIN, INPUT);\n  pinMode(IR_PIN, INPUT);\n  pinMode(LED1_PIN, OUTPUT);\n  pinMode(LED2_PIN, OUTPUT);\n  digitalWrite(LASER_PIN, HIGH); // Laser ON\n  digitalWrite(LED1_PIN, LOW);\n  digitalWrite(LED2_PIN, LOW);\n}\nvoid loop() {\n  int ldrState = digitalRead(LDR_PIN);\n  int irState = digitalRead(IR_PIN);\n  // IR usually LOW when object detected\n  bool irDetected = (irState == LOW);\n  bool beamBroken = (ldrState == LOW);\n  if (beamBroken || irDetected) {\n    // Alarm condition\n    digitalWrite(LED1_PIN, HIGH);\n    digitalWrite(LED2_PIN, HIGH);\n  } else {\n    // Normal condition\n    digitalWrite(LED1_PIN, LOW);\n    digitalWrite(LED2_PIN, LOW);\n  }\n}", "y2c9s9-explore-try-code": "#define LASER_PIN 2   // Laser diode on D2\n#define LDR_PIN 3     // LDR sensor on D3\n#define IR_PIN 4      // IR sensor on D4\n#define BUZZER_PIN 5  // Buzzer on D5\nvoid setup() {\n  pinMode(LASER_PIN, OUTPUT);\n  pinMode(LDR_PIN, INPUT);\n  pinMode(IR_PIN, INPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n  digitalWrite(LASER_PIN, HIGH); // Laser ON\n  digitalWrite(BUZZER_PIN, LOW); // Buzzer OFF\n}\nvoid loop() {\n  int ldrState = digitalRead(LDR_PIN);\n  int irState = digitalRead(IR_PIN);\n  bool irDetected = (irState == LOW);   // IR detection\n  bool beamBroken = (ldrState == LOW);  // Laser interruption\n  if (beamBroken || irDetected) {\n    digitalWrite(BUZZER_PIN, HIGH); // Alarm ON\n  } else {\n    digitalWrite(BUZZER_PIN, LOW);  // Alarm OFF\n  }\n}"});
  mergeCodes("2-9-9", "elaborate", {});

  if (content["2-9-1"] && content["2-9-1"].explore) {
    const pages = content["2-9-1"].explore.pages || [];
    for (const page of pages) {
      if (page[3] === "y2c9s1-explore-try-code") { page.pop(); page[1] = "Learning Page"; }
      if (String(page[0]).includes("page-13.jpg")) {
        page[1] = "Try This Yourself";
        page[2] = "Light Sensing & Auto Lighting source PDF practice code.";
        page[3] = "y2c9s1-explore-try-code";
      }
    }
  }

  const session7Pages = {"6": [["assets/images/pdf/year-2/class-6/session-7/explore-y2-c6-s7-page-01.jpg", "Project Setup", "Digital Display & Counting Logic source PDF page."], ["assets/images/pdf/year-2/class-6/session-7/explore-y2-c6-s7-page-02.jpg", "Learning Page", "Digital Display & Counting Logic source PDF page."], ["assets/images/pdf/year-2/class-6/session-7/explore-y2-c6-s7-page-03.jpg", "Main Code", "Digital Display & Counting Logic source PDF page.", "y2c6s7-explore-main-code"], ["assets/images/pdf/year-2/class-6/session-7/explore-y2-c6-s7-page-04.jpg", "Try This Yourself", "Digital Display & Counting Logic source PDF page.", "y2c6s7-explore-try-code"]], "7": [["assets/images/pdf/year-2/class-7/session-7/explore-y2-c7-s7-page-05.jpg", "Project Setup", "Digital Display & Counting Logic source PDF page."], ["assets/images/pdf/year-2/class-7/session-7/explore-y2-c7-s7-page-06.jpg", "Main Code", "Digital Display & Counting Logic source PDF page.", "y2c7s7-explore-main-code"], ["assets/images/pdf/year-2/class-7/session-7/explore-y2-c7-s7-page-07.jpg", "Learning Page", "Digital Display & Counting Logic source PDF page."], ["assets/images/pdf/year-2/class-7/session-7/explore-y2-c7-s7-page-08.jpg", "Try This Yourself", "Digital Display & Counting Logic source PDF page.", "y2c7s7-explore-try-code"], ["assets/images/pdf/year-2/class-7/session-7/explore-y2-c7-s7-page-09.jpg", "Learning Page", "Digital Display & Counting Logic source PDF page."]], "8": [["assets/images/pdf/year-2/class-8/session-7/explore-y2-c8-s7-page-10.jpg", "Project Setup", "Digital Display & Counting Logic source PDF page."], ["assets/images/pdf/year-2/class-8/session-7/explore-y2-c8-s7-page-11.jpg", "Main Code", "Digital Display & Counting Logic source PDF page.", "y2c8s7-explore-main-code"], ["assets/images/pdf/year-2/class-8/session-7/explore-y2-c8-s7-page-12.jpg", "Learning Page", "Digital Display & Counting Logic source PDF page."], ["assets/images/pdf/year-2/class-8/session-7/explore-y2-c8-s7-page-13.jpg", "Try This Yourself", "Digital Display & Counting Logic source PDF page.", "y2c8s7-explore-try-code"], ["assets/images/pdf/year-2/class-8/session-7/explore-y2-c8-s7-page-14.jpg", "Learning Page", "Digital Display & Counting Logic source PDF page."]], "9": [["assets/images/pdf/year-2/class-9/session-7/explore-y2-c9-s7-page-15.jpg", "Project Setup", "Digital Display & Counting Logic source PDF page."], ["assets/images/pdf/year-2/class-9/session-7/explore-y2-c9-s7-page-16.jpg", "Main Code", "Digital Display & Counting Logic source PDF page.", "y2c9s7-explore-main-code"], ["assets/images/pdf/year-2/class-9/session-7/explore-y2-c9-s7-page-17.jpg", "Learning Page", "Digital Display & Counting Logic source PDF page."], ["assets/images/pdf/year-2/class-9/session-7/explore-y2-c9-s7-page-18.jpg", "Try This Yourself", "Digital Display & Counting Logic source PDF page.", "y2c9s7-explore-try-code"], ["assets/images/pdf/year-2/class-9/session-7/explore-y2-c9-s7-page-19.jpg", "Learning Page", "Digital Display & Counting Logic source PDF page."], ["assets/images/pdf/year-2/class-9/session-7/explore-y2-c9-s7-page-20.jpg", "Learning Page", "Digital Display & Counting Logic source PDF page."]]};
  for (const grade of [6, 7, 8, 9]) {
    const lesson = content[`2-${grade}-7`];
    if (lesson && lesson.explore) lesson.explore.pages = session7Pages[String(grade)];
  }

  for (const grade of [6, 7, 8, 9]) {
    for (let session = 1; session <= 10; session += 1) {
      const lesson = content[`2-${grade}-${session}`];
      if (!lesson || !lesson.explore || !lesson.explain) continue;
      const codePage = (lesson.explore.pages || []).find((page) => page[3]);
      const code = codePage && lesson.explore.codes
        ? lesson.explore.codes[codePage[3]]
        : "";
      if (code) lesson.explain.code = code;
    }
  }
})();
